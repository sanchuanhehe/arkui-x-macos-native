diff --git a/ace_config.gni b/ace_config.gni
index 49645022..20305d09 100644
--- a/ace_config.gni
+++ b/ace_config.gni
@@ -277,7 +277,14 @@ if (defined(global_parts_info) &&
   ffrt_support = false
 }
 
-if (defined(global_parts_info.multimedia_player_framework) || is_arkui_x) {
+# Linux/Wayland native port: arkui-x linux has no multimedia player_framework
+# (media_client pulls a whole OHOS media stack — access_token/audio/av_codec/
+# drivers/samgr — none in the arkui-x linux component set). components_ng/render
+# already excludes use_linux from the player_framework dep; mirror that here so
+# components_ng/pattern and the ace_common PLAYER_FRAMEWORK_EXISTS define agree.
+# mac keeps it (its component set has player_framework).
+if ((defined(global_parts_info.multimedia_player_framework) || is_arkui_x) &&
+    !use_linux) {
   enable_player_framework = true
   ace_common_defines += [ "PLAYER_FRAMEWORK_EXISTS" ]
 } else {
diff --git a/adapter/preview/build/config_linux.gni b/adapter/preview/build/config_linux.gni
index 8393f4c6..8681c894 100644
--- a/adapter/preview/build/config_linux.gni
+++ b/adapter/preview/build/config_linux.gni
@@ -1,4 +1,4 @@
-# Copyright (c) 2021-2022 Huawei Device Co., Ltd.
+# Copyright (c) 2021-2026 Huawei Device Co., Ltd.
 # Licensed under the Apache License, Version 2.0 (the "License");
 # you may not use this file except in compliance with the License.
 # You may obtain a copy of the License at
@@ -12,10 +12,71 @@
 # limitations under the License.
 
 import("preview_common.gni")
+import(
+    "//foundation/graphic/graphic_2d/rosen/modules/render_service_base/config.gni")
 
-# linux platform defines and configs
+# linux platform defines and configs.
+# Route A (Wayland native): the arkui-x linux build is the real desktop app
+# backend (client-side EGL, like Android), NOT the GLFW previewer. We therefore
+# drop the previewer's GPU_DISABLED / preview font-mgr and wire the native
+# adapter/linux targets, mirroring adapter/preview/build/config_mac.gni. The
+# standalone (non-arkui-x) linux previewer keeps the old preview config.
+linux_use_wayland_backend = defined(is_arkui_x) && is_arkui_x
+
+defines += [ "LINUX_PLATFORM" ]
+
+# bounds_checking_function securectype.h leaves SECUREC_ENABLE_SNPRINTF/VSNPRINTF
+# at 0 on non-MSVC; securec.h then hides snprintf_s/vsnprintf_s that many libace
+# TUs call. The bounds_checking lib implements them — just enable the decls.
 defines += [
-  "LINUX_PLATFORM",
-  "GPU_DISABLED",
-  "SK_BUILD_FONT_MGR_FOR_PREVIEW_LINUX",
+  "SECUREC_ENABLE_SNPRINTF=1",
+  "SECUREC_ENABLE_VSNPRINTF=1",
 ]
+
+if (!linux_use_wayland_backend) {
+  # Standalone GLFW previewer path (unchanged).
+  defines += [
+    "GPU_DISABLED",
+    "SK_BUILD_FONT_MGR_FOR_PREVIEW_LINUX",
+  ]
+} else {
+  # Native Wayland desktop backend (client-side EGL). Reuse the proven
+  # client-side (iOS-style) code branches like the mac port does; LINUX_PLATFORM
+  # carries the real Wayland/EGL/POSIX divergences (parallel to mac's
+  # MAC_PLATFORM). Real freetype+fontconfig is skia's native Linux path, so no
+  # preview font-mgr is needed.
+  build_for_preview = false
+  use_external_icu = "static"
+
+  defines += [
+    "IOS_PLATFORM",
+    "CROSS_PLATFORM",
+    "NG_BUILD",
+    "VIRTUAL_RS_WINDOW",
+    "ENABLE_ROSEN_BACKEND",
+    "QUICK_PUSH_TRANSITION",
+    "ACE_ENABLE_GL",
+    "SUPPORT_TOUCH_TARGET_TEST",
+  ]
+
+  # Replace the previewer platform_deps (set in preview_common.gni) with the
+  # native-linux adapter targets.
+  platform_deps = []
+  platform_deps = [
+    "//foundation/arkui/ace_engine/adapter/linux/entrance:ace_linux_entrance",
+    "//foundation/arkui/ace_engine/adapter/linux/osal:ace_osal_linux",
+    "//foundation/arkui/ace_engine/adapter/linux/stage/ability:stage_linux_entrance",
+    "//foundation/arkui/ace_engine/adapter/linux/stage/uicontent:ace_uicontent_linux",
+  ]
+
+  # libace_static_linux is generated by ace_engine/build/BUILD.gn's foreach over
+  # ace_platforms; the linux platform_deps above are pulled in via ace_lib.gni.
+  libace_target = "//foundation/arkui/ace_engine/build:libace_static_linux"
+
+  # PREVIEW (from preview_common.gni) makes JsMessageDispatcher::CallCurlFunction
+  # a pure virtual that AceContainerSG never implements; drop it for the native
+  # backend.
+  if (filter_include(defines, [ "PREVIEW" ]) != []) {
+    defines -= [ "PREVIEW" ]
+  }
+}
diff --git a/adapter/preview/build/platform.gni b/adapter/preview/build/platform.gni
index eb25f004..65cfd67c 100644
--- a/adapter/preview/build/platform.gni
+++ b/adapter/preview/build/platform.gni
@@ -41,6 +41,13 @@ if (use_mac) {
 if (use_linux) {
   linux_platform = {
     name = "linux"
+
+    # Linux/Wayland native port: admit the linux platform into ace_platforms under
+    # is_arkui_x (the foreach filter requires cross_platform_support). This makes
+    # skia_linux / 2d_graphics_source_linux / libace_static_linux etc. get
+    # generated for target_os=linux, mirroring the mac port. Route A: real stage
+    # app + Android-style client-side EGL, not the GLFW preview path.
+    cross_platform_support = true
     config = {
       import("config_linux.gni")
     }
diff --git a/frameworks/base/log/ace_scoring_log.h b/frameworks/base/log/ace_scoring_log.h
index d0a4c8ed..3f64c56e 100644
--- a/frameworks/base/log/ace_scoring_log.h
+++ b/frameworks/base/log/ace_scoring_log.h
@@ -20,6 +20,8 @@
 
 #include "base/utils/macros.h"
 #include "base/utils/noncopyable.h"
+// [linux-port] strict libc++: explicit std includes (mac clang pulled these in transitively)
+#include <cstdint>
 
 #define ACE_SCORING_EVENT(event) AceScoringLog AceScoringLog(event)
 #define ACE_SCORING_COMPONENT(page, component, proc) AceScoringLog AceScoringLog(page, component, proc)
diff --git a/frameworks/base/log/jank_frame_report.h b/frameworks/base/log/jank_frame_report.h
index 9094c72b..17f8760f 100644
--- a/frameworks/base/log/jank_frame_report.h
+++ b/frameworks/base/log/jank_frame_report.h
@@ -20,6 +20,8 @@
 
 #include <string>
 #include <vector>
+// [linux-port] strict libc++: explicit std includes (mac clang pulled these in transitively)
+#include <cstdint>
 
 namespace OHOS::Ace {
 using JankFrameFlag = uint32_t;
diff --git a/frameworks/base/thread/background_task_executor.cpp b/frameworks/base/thread/background_task_executor.cpp
index 670e7114..bbcec9b7 100644
--- a/frameworks/base/thread/background_task_executor.cpp
+++ b/frameworks/base/thread/background_task_executor.cpp
@@ -32,7 +32,7 @@ void SetThreadName(uint32_t threadNo)
 {
     std::string name("ace.bg.");
     name.append(std::to_string(threadNo));
-#if defined(MAC_PLATFORM) || defined(IOS_PLATFORM)
+#if (defined(MAC_PLATFORM) || defined(IOS_PLATFORM)) && !defined(LINUX_PLATFORM)
     pthread_setname_np(name.c_str());
 #else
     pthread_setname_np(pthread_self(), name.c_str());
diff --git a/frameworks/base/utils/feature_param.h b/frameworks/base/utils/feature_param.h
index 247a32c2..6f278721 100644
--- a/frameworks/base/utils/feature_param.h
+++ b/frameworks/base/utils/feature_param.h
@@ -19,6 +19,8 @@
 #include <string>
 
 #include "ui/base/macros.h"
+// [linux-port] strict libc++: explicit std includes (mac clang pulled these in transitively)
+#include <cstdint>
 
 namespace OHOS::Ace {
 class ACE_FORCE_EXPORT FeatureParam {
diff --git a/frameworks/bridge/common/utils/module_buffer_reader.h b/frameworks/bridge/common/utils/module_buffer_reader.h
index cdd61e65..29f0f02c 100644
--- a/frameworks/bridge/common/utils/module_buffer_reader.h
+++ b/frameworks/bridge/common/utils/module_buffer_reader.h
@@ -20,6 +20,8 @@
 #include <string>
 
 #include "base/utils/singleton.h"
+// [linux-port] strict libc++: explicit std includes (mac clang pulled these in transitively)
+#include <cstdint>
 
 namespace OHOS::Ace::Framework {
 
diff --git a/frameworks/bridge/declarative_frontend/BUILD.gn b/frameworks/bridge/declarative_frontend/BUILD.gn
index a9ff3c0d..58f91e75 100644
--- a/frameworks/bridge/declarative_frontend/BUILD.gn
+++ b/frameworks/bridge/declarative_frontend/BUILD.gn
@@ -74,10 +74,16 @@ template("declarative_frontend") {
       external_deps += [ "hilog:libhilog" ]
     }
     if (!build_ohos_sdk) {
-      external_deps += [
-        "c_utils:utils",
-        "ipc:ipc_core",
-      ]
+      external_deps += [ "c_utils:utils" ]
+
+      # Linux/Wayland native port: ipc (binder) isn't a part in the arkui-x linux
+      # component set (it is on other platforms), so referencing ipc:ipc_core
+      # unconditionally fails gn resolution here. Only add it when the ipc part
+      # is actually present (same defensive pattern as enable_player_framework).
+      if (defined(global_parts_info) &&
+          defined(global_parts_info.communication_ipc)) {
+        external_deps += [ "ipc:ipc_core" ]
+      }
     }
   }
 }
@@ -692,7 +698,10 @@ template("declarative_js_engine") {
       ]
     }
 
-    if (platform == "windows" || platform == "mac" || platform == "linux") {
+    if ((platform == "windows" || platform == "mac" || platform == "linux") &&
+        !is_arkui_x) {
+      # Linux/Wayland native port: js_mock_system_plugin is a previewer-only gen
+      # target (defined under !is_arkui_x); the arkui-x desktop app doesn't use it.
       deps += [ ":gen_obj_src_js_mock_system_plugin" ]
     } else {
       if (defined(config.plugin_components_support) &&
diff --git a/frameworks/bridge/declarative_frontend/ark_node/package-lock.json b/frameworks/bridge/declarative_frontend/ark_node/package-lock.json
index 2dda58bf..fd687453 100644
--- a/frameworks/bridge/declarative_frontend/ark_node/package-lock.json
+++ b/frameworks/bridge/declarative_frontend/ark_node/package-lock.json
@@ -1,38 +1,20 @@
 {
   "name": "ark_xnode",
   "version": "0.0.1",
-  "lockfileVersion": 3,
+  "lockfileVersion": 1,
   "requires": true,
-  "packages": {
-    "": {
-      "name": "ark_xnode",
-      "version": "0.0.1",
-      "license": "Apache-2.0",
-      "devDependencies": {
-        "@types/node": "^15.0.1",
-        "typescript": "^4.2.4"
-      }
-    },
-    "node_modules/@types/node": {
+  "dependencies": {
+    "@types/node": {
       "version": "15.14.9",
       "resolved": "https://repo.huaweicloud.com/repository/npm/@types/node/-/node-15.14.9.tgz",
       "integrity": "sha512-qjd88DrCxupx/kJD5yQgZdcYKZKSIGBVDIBE1/LTGcNm3d2Np/jxojkdePDdfnBHJc5W7vSMpbJ1aB7p/Py69A==",
-      "dev": true,
-      "license": "MIT"
+      "dev": true
     },
-    "node_modules/typescript": {
+    "typescript": {
       "version": "4.9.5",
       "resolved": "https://repo.huaweicloud.com/repository/npm/typescript/-/typescript-4.9.5.tgz",
       "integrity": "sha512-1FXk9E2Hm+QzZQ7z+McJiHL4NW1F2EzMu9Nq9i3zAaGqibafqYwCVU6WyWAuyQRRzOlxou8xZSyXLEN8oKj24g==",
-      "dev": true,
-      "license": "Apache-2.0",
-      "bin": {
-        "tsc": "bin/tsc",
-        "tsserver": "bin/tsserver"
-      },
-      "engines": {
-        "node": ">=4.2.0"
-      }
+      "dev": true
     }
   }
 }
diff --git a/frameworks/bridge/declarative_frontend/ark_theme/theme_manager/package-lock.json b/frameworks/bridge/declarative_frontend/ark_theme/theme_manager/package-lock.json
index 11da427d..61bbedd0 100644
--- a/frameworks/bridge/declarative_frontend/ark_theme/theme_manager/package-lock.json
+++ b/frameworks/bridge/declarative_frontend/ark_theme/theme_manager/package-lock.json
@@ -1,38 +1,20 @@
 {
   "name": "ark_theme",
   "version": "0.0.1",
-  "lockfileVersion": 3,
+  "lockfileVersion": 1,
   "requires": true,
-  "packages": {
-    "": {
-      "name": "ark_theme",
-      "version": "0.0.1",
-      "license": "Apache-2.0",
-      "devDependencies": {
-        "@types/node": "^15.0.1",
-        "typescript": "^4.2.4"
-      }
-    },
-    "node_modules/@types/node": {
+  "dependencies": {
+    "@types/node": {
       "version": "15.14.9",
       "resolved": "https://repo.huaweicloud.com/repository/npm/@types/node/-/node-15.14.9.tgz",
       "integrity": "sha512-qjd88DrCxupx/kJD5yQgZdcYKZKSIGBVDIBE1/LTGcNm3d2Np/jxojkdePDdfnBHJc5W7vSMpbJ1aB7p/Py69A==",
-      "dev": true,
-      "license": "MIT"
+      "dev": true
     },
-    "node_modules/typescript": {
+    "typescript": {
       "version": "4.9.5",
       "resolved": "https://repo.huaweicloud.com/repository/npm/typescript/-/typescript-4.9.5.tgz",
       "integrity": "sha512-1FXk9E2Hm+QzZQ7z+McJiHL4NW1F2EzMu9Nq9i3zAaGqibafqYwCVU6WyWAuyQRRzOlxou8xZSyXLEN8oKj24g==",
-      "dev": true,
-      "license": "Apache-2.0",
-      "bin": {
-        "tsc": "bin/tsc",
-        "tsserver": "bin/tsserver"
-      },
-      "engines": {
-        "node": ">=4.2.0"
-      }
+      "dev": true
     }
   }
 }
diff --git a/frameworks/core/accessibility/utils/accessibility_manager_utils.h b/frameworks/core/accessibility/utils/accessibility_manager_utils.h
index a1d3b4e9..331217a4 100644
--- a/frameworks/core/accessibility/utils/accessibility_manager_utils.h
+++ b/frameworks/core/accessibility/utils/accessibility_manager_utils.h
@@ -183,7 +183,7 @@ public:
 
     void AddAccessibilityEvent(const int32_t containerId, const int32_t pageId, AccessibilityEvent& event)
     {
-        TimeStamp now = std::chrono::steady_clock::now();
+        TimeStamp now = std::chrono::high_resolution_clock::now();
         auto& eventList = pageEvent_[containerId];
         eventList.emplace_back(std::make_tuple(pageId, event, now));
 
diff --git a/frameworks/core/common/ime/text_input_formatter.h b/frameworks/core/common/ime/text_input_formatter.h
index 859875c2..92df9f1f 100644
--- a/frameworks/core/common/ime/text_input_formatter.h
+++ b/frameworks/core/common/ime/text_input_formatter.h
@@ -18,6 +18,8 @@
 
 #include <regex>
 #include "base/utils/macros.h"
+// [linux-port] strict libc++: explicit std includes (mac clang pulled these in transitively)
+#include <cstdint>
 
 namespace OHOS::Ace {
 
diff --git a/frameworks/core/common/multi_thread_build_manager.h b/frameworks/core/common/multi_thread_build_manager.h
index 29c1924a..9642cb21 100644
--- a/frameworks/core/common/multi_thread_build_manager.h
+++ b/frameworks/core/common/multi_thread_build_manager.h
@@ -22,6 +22,8 @@
 
 #include "base/utils/macros.h"
 #include "base/utils/noncopyable.h"
+// [linux-port] strict libc++: explicit std includes (mac clang pulled these in transitively)
+#include <string>
 
 namespace OHOS::Ace {
 namespace NG {
diff --git a/frameworks/core/components/text_overlay/BUILD.gn b/frameworks/core/components/text_overlay/BUILD.gn
index b5e9bccc..95f36b44 100755
--- a/frameworks/core/components/text_overlay/BUILD.gn
+++ b/frameworks/core/components/text_overlay/BUILD.gn
@@ -48,8 +48,8 @@ build_component("text_overlay_for_web") {
 
   standard_web_deps = [ "webview:libnweb" ]
   external_deps += [
-    "ability_runtime:abilitykit_native",
-    "ability_runtime:app_manager",
-    "window_manager:libwm",
+    # [linux-port auto-gate] "ability_runtime:abilitykit_native",
+    # [linux-port auto-gate] "ability_runtime:app_manager",
+    # [linux-port auto-gate] "window_manager:libwm",
   ]
 }
diff --git a/frameworks/core/components_ng/render/adapter/rosen_render_context.cpp b/frameworks/core/components_ng/render/adapter/rosen_render_context.cpp
index 6b419089..c016777d 100644
--- a/frameworks/core/components_ng/render/adapter/rosen_render_context.cpp
+++ b/frameworks/core/components_ng/render/adapter/rosen_render_context.cpp
@@ -7718,7 +7718,7 @@ void RosenRenderContext::MarkNewFrameAvailable(void* nativeWindow)
 #if defined(ANDROID_PLATFORM)
     rsSurfaceNode->MarkUiFrameAvailable(true);
 #endif
-#if defined(IOS_PLATFORM)
+#if defined(IOS_PLATFORM) && !defined(LINUX_PLATFORM)
 #if defined(PLATFORM_VIEW_SUPPORTED)
     if (patternType_ == PatternType::PLATFORM_VIEW || patternType_ == PatternType::XCOM) {
         RSSurfaceExtConfig config = {
@@ -7742,7 +7742,7 @@ void RosenRenderContext::AddAttachCallBack(const std::function<void(int64_t, boo
 {
     FREE_RS_CONTEXT_CHECK(AddAttachCallBack, attachCallback);
     CHECK_NULL_VOID(rsNode_);
-#if defined(ANDROID_PLATFORM) || defined(IOS_PLATFORM)
+#if (defined(ANDROID_PLATFORM) || defined(IOS_PLATFORM)) && !defined(LINUX_PLATFORM)
     auto rsSurfaceNode = rsNode_->ReinterpretCastTo<Rosen::RSSurfaceNode>();
     CHECK_NULL_VOID(rsSurfaceNode);
     rsSurfaceNode->SetSurfaceTextureAttachCallBack(attachCallback);
@@ -7753,7 +7753,7 @@ void RosenRenderContext::AddUpdateCallBack(const std::function<void(std::vector<
 {
     FREE_RS_CONTEXT_CHECK(AddUpdateCallBack, updateCallback);
     CHECK_NULL_VOID(rsNode_);
-#if defined(ANDROID_PLATFORM) || defined(IOS_PLATFORM)
+#if (defined(ANDROID_PLATFORM) || defined(IOS_PLATFORM)) && !defined(LINUX_PLATFORM)
     auto rsSurfaceNode = rsNode_->ReinterpretCastTo<Rosen::RSSurfaceNode>();
     CHECK_NULL_VOID(rsSurfaceNode);
     rsSurfaceNode->SetSurfaceTextureUpdateCallBack(updateCallback);
@@ -7764,7 +7764,7 @@ void RosenRenderContext::AddInitTypeCallBack(const std::function<void(int32_t&)>
 {
     FREE_RS_CONTEXT_CHECK(AddInitTypeCallBack, initTypeCallback);
     CHECK_NULL_VOID(rsNode_);
-#if defined(ANDROID_PLATFORM) || defined(IOS_PLATFORM)
+#if (defined(ANDROID_PLATFORM) || defined(IOS_PLATFORM)) && !defined(LINUX_PLATFORM)
     auto rsSurfaceNode = rsNode_->ReinterpretCastTo<Rosen::RSSurfaceNode>();
     CHECK_NULL_VOID(rsSurfaceNode);
     rsSurfaceNode->SetSurfaceTextureInitTypeCallBack(initTypeCallback);
diff --git a/frameworks/core/components_ng/render/adapter/rosen_window.h b/frameworks/core/components_ng/render/adapter/rosen_window.h
index ae41531d..b116643f 100644
--- a/frameworks/core/components_ng/render/adapter/rosen_window.h
+++ b/frameworks/core/components_ng/render/adapter/rosen_window.h
@@ -23,7 +23,9 @@
 #include "wm/window.h"
 #elif VIRTUAL_RS_WINDOW
 // use real rs window later
-#ifdef IOS_PLATFORM
+#if defined(LINUX_PLATFORM)
+#include "adapter/linux/entrance/virtual_rs_window.h"
+#elif defined(IOS_PLATFORM)
 #include "adapter/ios/entrance/virtual_rs_window.h"
 #else
 #include "adapter/android/entrance/java/jni/virtual_rs_window.h"
diff --git a/interfaces/inner_api/ace/ace_forward_compatibility.h b/interfaces/inner_api/ace/ace_forward_compatibility.h
index 19197c09..259b06ec 100644
--- a/interfaces/inner_api/ace/ace_forward_compatibility.h
+++ b/interfaces/inner_api/ace/ace_forward_compatibility.h
@@ -18,6 +18,8 @@
 
 #include <string>
 #include "macros.h"
+// [linux-port] strict libc++: explicit std includes (mac clang pulled these in transitively)
+#include <cstdint>
 
 namespace OHOS {
 namespace Ace {
diff --git a/interfaces/inner_api/ace_kit/BUILD.gn b/interfaces/inner_api/ace_kit/BUILD.gn
index ae417bc2..53349bc1 100644
--- a/interfaces/inner_api/ace_kit/BUILD.gn
+++ b/interfaces/inner_api/ace_kit/BUILD.gn
@@ -30,6 +30,9 @@ ohos_source_set("ace_kit") {
     "USE_ARK_ENGINE",
     "NOGDI",
   ]
+  if (use_linux) {
+    defines += [ "LINUX_PLATFORM" ]
+  }
 
   cflags_cc = [ "-Wno-macro-redefined" ]
 
diff --git a/interfaces/inner_api/ace_kit/include/ui/base/inspector_filter.h b/interfaces/inner_api/ace_kit/include/ui/base/inspector_filter.h
index c7e1563f..cea0c86e 100644
--- a/interfaces/inner_api/ace_kit/include/ui/base/inspector_filter.h
+++ b/interfaces/inner_api/ace_kit/include/ui/base/inspector_filter.h
@@ -22,6 +22,8 @@
 #include <vector>
 
 #include "ui/base/macros.h"
+// [linux-port] strict libc++: explicit std includes (mac clang pulled these in transitively)
+#include <cstdint>
 
 namespace OHOS::Ace::NG {
 namespace TreeKey {
diff --git a/interfaces/inner_api/ace_kit/include/ui/base/type_info_base.h b/interfaces/inner_api/ace_kit/include/ui/base/type_info_base.h
index 89b14def..def03a6c 100644
--- a/interfaces/inner_api/ace_kit/include/ui/base/type_info_base.h
+++ b/interfaces/inner_api/ace_kit/include/ui/base/type_info_base.h
@@ -20,6 +20,8 @@
 #include <type_traits>
 
 #include "ui/base/memory_monitor_def.h"
+// [linux-port] strict libc++: explicit std includes (mac clang pulled these in transitively)
+#include <cstdint>
 
 // Generate 'TypeInfo' for each classes.
 // And using hash code of its name for 'TypeId'.
diff --git a/interfaces/inner_api/drawable_descriptor/BUILD.gn b/interfaces/inner_api/drawable_descriptor/BUILD.gn
index e58f8e0b..9c10cf95 100644
--- a/interfaces/inner_api/drawable_descriptor/BUILD.gn
+++ b/interfaces/inner_api/drawable_descriptor/BUILD.gn
@@ -14,6 +14,10 @@
 import("//build/ohos.gni")
 import("//foundation/arkui/ace_engine/ace_config.gni")
 
+# Linux/Wayland native port: ace_enable_gpu (used below) is defined in graphic_2d's
+# graphic_config.gni, which ace_config.gni doesn't pull in here.
+import("//foundation/graphic/graphic_2d/graphic_config.gni")
+
 config("drawable_config") {
   visibility = [ ":*" ]
   include_dirs = [
@@ -64,6 +68,16 @@ if (is_arkui_x) {
     if (use_clang_ios) {
       defines = image_decode_ios_defines
     }
+    if (current_os == "linux") {
+      # Linux/Wayland native port: mirror android/ios (ANDROID_PLATFORM/IOS_PLATFORM)
+      # with the generic CROSS_PLATFORM marker. pixel_map.h then skips
+      # pixel_map_parcel.h -> "message_parcel.h" (no foundation/communication/ipc on
+      # linux), so the image mock's parcel.h/nocopyable.h are never pulled in and no
+      # longer collide with c_utils' real parcel.h/nocopyable.h (redefinition of
+      # NoCopyable/Parcelable). Also initializes `defines` (previously only set for
+      # android/ios) for the later `defines +=` (ace_enable_gpu / upgrade_skia).
+      defines = [ "CROSS_PLATFORM" ]
+    }
     if (ace_enable_gpu) {
       defines += [ "RS_ENABLE_GPU" ]
     }
diff --git a/test/mock/ohos_mock/ipc_single/iremote_object.h b/test/mock/ohos_mock/ipc_single/iremote_object.h
index 3895109e..804605d3 100644
--- a/test/mock/ohos_mock/ipc_single/iremote_object.h
+++ b/test/mock/ohos_mock/ipc_single/iremote_object.h
@@ -19,8 +19,18 @@
 #include <string>
 
 #include "refbase.h"
+#include "errors.h"  // [linux-port] ErrCode/ERR_OK for IDL-generated IPC sources
 
 namespace OHOS {
+// [linux-port] IDL-generated IPC code (qos_manager concurrent_task *_types/*_proxy)
+// expects MIN_TRANSACTION_ID / ERR_INVALID_DATA / ERR_NONE from the real
+// ipc_types.h / iremote_object.h. There is no foundation/communication/ipc repo on
+// linux and this cross-platform IPC mock omits them, so provide minimal definitions.
+constexpr int MIN_TRANSACTION_ID = 0x00000001;
+enum {
+    ERR_NONE = 0,
+    ERR_INVALID_DATA = 5,
+};
 class IRemoteObject {
 public:
     explicit IRemoteObject(const std::u16string& descriptor = std::u16string()) {}
diff --git a/test/mock/ohos_mock/ipc_single/message_parcel.h b/test/mock/ohos_mock/ipc_single/message_parcel.h
index 28d43f09..f0abbe30 100644
--- a/test/mock/ohos_mock/ipc_single/message_parcel.h
+++ b/test/mock/ohos_mock/ipc_single/message_parcel.h
@@ -23,6 +23,8 @@
 #include "iremote_object.h"
 #include "parcel.h"
 #include "refbase.h"
+// [linux-port] strict libc++: explicit std includes (mac clang pulled these in transitively)
+#include <memory>
 
 namespace OHOS {
 class MessageParcel : public Parcel {
