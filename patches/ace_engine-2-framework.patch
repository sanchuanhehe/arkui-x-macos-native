diff --git a/frameworks/bridge/declarative_frontend/BUILD.gn b/frameworks/bridge/declarative_frontend/BUILD.gn
index 20b15499ef9..6002ad91115 100644
--- a/frameworks/bridge/declarative_frontend/BUILD.gn
+++ b/frameworks/bridge/declarative_frontend/BUILD.gn
@@ -704,7 +704,10 @@ template("declarative_js_engine") {
       ]
     }
 
-    if (platform == "windows" || platform == "mac" || platform == "linux") {
+    if ((platform == "windows" || platform == "mac" || platform == "linux") &&
+        !is_arkui_x) {
+      # Phase 2 path X: js_mock_system_plugin is a previewer-only gen target
+      # (defined under !is_arkui_x); the arkui-x mac app doesn't use it.
       deps += [ ":gen_obj_src_js_mock_system_plugin" ]
     } else {
       if (defined(config.plugin_components_support) &&
diff --git a/frameworks/bridge/declarative_frontend/ark_component/src/ArkComponent.ts b/frameworks/bridge/declarative_frontend/ark_component/src/ArkComponent.ts
index 4e90f98e220..e5e6bfee75f 100644
--- a/frameworks/bridge/declarative_frontend/ark_component/src/ArkComponent.ts
+++ b/frameworks/bridge/declarative_frontend/ark_component/src/ArkComponent.ts
@@ -6175,6 +6175,13 @@ class ArkComponent {
   }
 }
 (globalThis as any).__ArkComponent__ = ArkComponent;
+// 把 ModifierWithKey 暴露到 globalThis:部分组件(columnsplit/rowsplit/hyperlink/menu/
+// waterflow 等)的 ark<x>.js 在模块顶层 `class XModifier extends ModifierWithKey`,模块加载即
+// 求值,而非像 search/marquee 那样延迟到 loadComponent()。这些组件作为 native module abc 经
+// require() 执行时,若 ModifierWithKey 不是全局,顶层 extends 立即抛 ReferenceError →
+// "Cannot execute panda file from memory",整个 abc 执行失败、组件不渲染。挂到全局即修复
+// 全部这类「旧模式」组件(iOS 因 abc 执行上下文不同未暴露此问题)。
+(globalThis as any).ModifierWithKey = ModifierWithKey;
 
 const isNull = (val: any) => typeof val === 'object' && val === null;
 const isArray = (val: any) => Array.isArray(val);
diff --git a/frameworks/bridge/declarative_frontend/engine/bindings_implementation.cpp b/frameworks/bridge/declarative_frontend/engine/bindings_implementation.cpp
index 3d1be9d56f3..47345bc9fac 100644
--- a/frameworks/bridge/declarative_frontend/engine/bindings_implementation.cpp
+++ b/frameworks/bridge/declarative_frontend/engine/bindings_implementation.cpp
@@ -17,7 +17,7 @@
 
 namespace OHOS::Ace::Framework {
 
-    thread_local std::unordered_map<void*, std::vector<std::unique_ptr<IFunctionBinding>>> IFunctionBinding::functions;
-    thread_local void* IFunctionBinding::runtime = nullptr;
+    ACE_JSI_THREAD_LOCAL std::unordered_map<void*, std::vector<std::unique_ptr<IFunctionBinding>>> IFunctionBinding::functions;
+    ACE_JSI_THREAD_LOCAL void* IFunctionBinding::runtime = nullptr;
 
 }; // namespace OHOS::Ace::Framework
diff --git a/frameworks/bridge/declarative_frontend/engine/bindings_implementation.h b/frameworks/bridge/declarative_frontend/engine/bindings_implementation.h
index 0b5f5b15ce7..a4f6ee97f7b 100644
--- a/frameworks/bridge/declarative_frontend/engine/bindings_implementation.h
+++ b/frameworks/bridge/declarative_frontend/engine/bindings_implementation.h
@@ -16,6 +16,18 @@
 #ifndef FOUNDATION_ACE_FRAMEWORKS_DECLARATIVE_FRONTEND_ENGINE_BINDINGS_IMPLEMENTATION_H
 #define FOUNDATION_ACE_FRAMEWORKS_DECLARATIVE_FRONTEND_ENGINE_BINDINGS_IMPLEMENTATION_H
 
+// Phase 0 path X (M1 link): on macOS, `ACE_JSI_THREAD_LOCAL` template static members
+// (JsiClass<T>::className_/jsName/staticFunctions_/...) emit a strong, per-TU
+// TLS-init helper symbol that the Apple linker refuses to coalesce, producing
+// "duplicate symbol" for every JS binding type instantiated in >1 TU. The
+// desktop app drives the JS engine from a single thread, so use a plain static
+// (clang emits it as a coalesced weak symbol) on mac.
+#if defined(MAC_PLATFORM)
+#define ACE_JSI_THREAD_LOCAL
+#else
+#define ACE_JSI_THREAD_LOCAL thread_local
+#endif
+
 #include <cmath>
 #include <string>
 #include <unordered_map>
@@ -35,8 +47,8 @@ enum MethodOptions : uint8_t {
 
 class IFunctionBinding {
 public:
-    thread_local static std::unordered_map<void*, std::vector<std::unique_ptr<IFunctionBinding>>> functions;
-    thread_local static void* runtime;
+    ACE_JSI_THREAD_LOCAL static std::unordered_map<void*, std::vector<std::unique_ptr<IFunctionBinding>>> functions;
+    ACE_JSI_THREAD_LOCAL static void* runtime;
     IFunctionBinding(const char* name, MethodOptions options) : name_(name), options_(options)
     {
         functions[runtime].emplace_back(this);
@@ -396,7 +408,7 @@ public:
     static JSRef<JSObject> NewInstance();
 
 private:
-    static thread_local std::string jsName;
+    static ACE_JSI_THREAD_LOCAL std::string jsName;
 };
 
 }; // namespace OHOS::Ace::Framework
diff --git a/frameworks/bridge/declarative_frontend/engine/bindings_implementation.inl b/frameworks/bridge/declarative_frontend/engine/bindings_implementation.inl
index 65dfcf561ef..39149a607a1 100644
--- a/frameworks/bridge/declarative_frontend/engine/bindings_implementation.inl
+++ b/frameworks/bridge/declarative_frontend/engine/bindings_implementation.inl
@@ -18,7 +18,7 @@
 namespace OHOS::Ace::Framework {
 
 template<typename T, template<typename> typename ImplDetail>
-thread_local std::string JSClassImpl<T, ImplDetail>::jsName;
+ACE_JSI_THREAD_LOCAL std::string JSClassImpl<T, ImplDetail>::jsName;
 
 template<typename C, template<typename> typename ImplDetail>
 void JSClassImpl<C, ImplDetail>::Declare(const char* name)
diff --git a/frameworks/bridge/declarative_frontend/engine/jsi/jsi_bindings.h b/frameworks/bridge/declarative_frontend/engine/jsi/jsi_bindings.h
index 7d4b11d1dfb..f3143a20181 100644
--- a/frameworks/bridge/declarative_frontend/engine/jsi/jsi_bindings.h
+++ b/frameworks/bridge/declarative_frontend/engine/jsi/jsi_bindings.h
@@ -130,14 +130,14 @@ private:
 
     static bool CheckIfConstructCall(panda::JsiRuntimeCallInfo* runtimeCallInfo);
 
-    static thread_local std::unordered_map<std::string, panda::Global<panda::FunctionRef>> staticFunctions_;
-    static thread_local std::unordered_map<std::string, panda::Global<panda::FunctionRef>> customFunctions_;
-    static thread_local std::unordered_map<std::string, panda::Global<panda::FunctionRef>> customGetFunctions_;
-    static thread_local std::unordered_map<std::string, panda::Global<panda::FunctionRef>> customSetFunctions_;
-    static thread_local FunctionCallback constructor_;
-    static thread_local JsiClassConstructorBinding jsConstructorBinding_;
-    static thread_local std::string className_;
-    static thread_local panda::Global<panda::FunctionRef> classFunction_;
+    static ACE_JSI_THREAD_LOCAL std::unordered_map<std::string, panda::Global<panda::FunctionRef>> staticFunctions_;
+    static ACE_JSI_THREAD_LOCAL std::unordered_map<std::string, panda::Global<panda::FunctionRef>> customFunctions_;
+    static ACE_JSI_THREAD_LOCAL std::unordered_map<std::string, panda::Global<panda::FunctionRef>> customGetFunctions_;
+    static ACE_JSI_THREAD_LOCAL std::unordered_map<std::string, panda::Global<panda::FunctionRef>> customSetFunctions_;
+    static ACE_JSI_THREAD_LOCAL FunctionCallback constructor_;
+    static ACE_JSI_THREAD_LOCAL JsiClassConstructorBinding jsConstructorBinding_;
+    static ACE_JSI_THREAD_LOCAL std::string className_;
+    static ACE_JSI_THREAD_LOCAL panda::Global<panda::FunctionRef> classFunction_;
 };
 
 template<typename T>
diff --git a/frameworks/bridge/declarative_frontend/engine/jsi/jsi_bindings.inl b/frameworks/bridge/declarative_frontend/engine/jsi/jsi_bindings.inl
index 85895498d4e..c94abc792bd 100644
--- a/frameworks/bridge/declarative_frontend/engine/jsi/jsi_bindings.inl
+++ b/frameworks/bridge/declarative_frontend/engine/jsi/jsi_bindings.inl
@@ -24,28 +24,28 @@
 namespace OHOS::Ace::Framework {
 
 template<typename C>
-thread_local std::unordered_map<std::string, panda::Global<panda::FunctionRef>> JsiClass<C>::staticFunctions_;
+ACE_JSI_THREAD_LOCAL std::unordered_map<std::string, panda::Global<panda::FunctionRef>> JsiClass<C>::staticFunctions_;
 
 template<typename C>
-thread_local std::unordered_map<std::string, panda::Global<panda::FunctionRef>> JsiClass<C>::customFunctions_;
+ACE_JSI_THREAD_LOCAL std::unordered_map<std::string, panda::Global<panda::FunctionRef>> JsiClass<C>::customFunctions_;
 
 template<typename C>
-thread_local std::unordered_map<std::string, panda::Global<panda::FunctionRef>> JsiClass<C>::customGetFunctions_;
+ACE_JSI_THREAD_LOCAL std::unordered_map<std::string, panda::Global<panda::FunctionRef>> JsiClass<C>::customGetFunctions_;
 
 template<typename C>
-thread_local std::unordered_map<std::string, panda::Global<panda::FunctionRef>> JsiClass<C>::customSetFunctions_;
+ACE_JSI_THREAD_LOCAL std::unordered_map<std::string, panda::Global<panda::FunctionRef>> JsiClass<C>::customSetFunctions_;
 
 template<typename C>
-thread_local FunctionCallback JsiClass<C>::constructor_ = nullptr;
+ACE_JSI_THREAD_LOCAL FunctionCallback JsiClass<C>::constructor_ = nullptr;
 
 template<typename C>
-thread_local JsiClassConstructorBinding JsiClass<C>::jsConstructorBinding_;
+ACE_JSI_THREAD_LOCAL JsiClassConstructorBinding JsiClass<C>::jsConstructorBinding_;
 
 template<typename C>
-thread_local std::string JsiClass<C>::className_;
+ACE_JSI_THREAD_LOCAL std::string JsiClass<C>::className_;
 
 template<typename C>
-thread_local panda::Global<panda::FunctionRef> JsiClass<C>::classFunction_;
+ACE_JSI_THREAD_LOCAL panda::Global<panda::FunctionRef> JsiClass<C>::classFunction_;
 
 template<typename C>
 void JsiClass<C>::Declare(const char* name)
diff --git a/frameworks/bridge/declarative_frontend/engine/jsi/jsi_declarative_engine.cpp b/frameworks/bridge/declarative_frontend/engine/jsi/jsi_declarative_engine.cpp
index 93b946c130b..ad097cb1d81 100644
--- a/frameworks/bridge/declarative_frontend/engine/jsi/jsi_declarative_engine.cpp
+++ b/frameworks/bridge/declarative_frontend/engine/jsi/jsi_declarative_engine.cpp
@@ -242,49 +242,87 @@ bool EvaluateAbcFile(const shared_ptr<JsRuntime>& runtime, const std::string& fi
     return arkRuntime->EvaluateJsCode(content.data(), static_cast<int32_t>(content.size()), filePath);
 }
 
+// Run a preload-abc evaluation at most once per runtime. These framework abc modules
+// (stateMgmt / jsEnumStyle / arkCommon / arkComponent / ...) declare top-level lexical
+// bindings (const / class). On the cross-platform shared-runtime path the preload
+// helpers are reachable more than once for a single runtime (InitAceModule from a page
+// engine + PreloadAceModule on the shared global runtime). Re-evaluating then throws
+// "Duplicate identifier ..." which aborts func_main_0 and leaves the module's globals
+// (e.g. LogTag, stateMgmtConsole) half-initialized, later crashing form components in
+// observeComponentCreation2. Guard with a per-runtime sentinel on the global object
+// (each runtime has its own global, so a freshly created runtime still loads normally).
+template<typename EvalFn>
+inline bool PreloadAbcOnce(const shared_ptr<JsRuntime>& runtime, const char* sentinel, EvalFn&& evalFn)
+{
+    shared_ptr<JsValue> global = runtime->GetGlobal();
+    if (global != nullptr) {
+        shared_ptr<JsValue> loaded = global->GetProperty(runtime, sentinel);
+        if (loaded != nullptr && loaded->IsBoolean(runtime) && loaded->ToBoolean(runtime)) {
+            return true;
+        }
+    }
+    bool result = evalFn();
+    if (result && global != nullptr) {
+        global->SetProperty(runtime, sentinel, runtime->NewBoolean(true));
+    }
+    return result;
+}
+
 inline bool PreloadJsEnums(const shared_ptr<JsRuntime>& runtime)
 {
-    return EvaluateAbcFile(runtime, NG::GetSystemPath("jsEnumStyle.abc"));
+    return PreloadAbcOnce(runtime, "__jsEnumsLoaded__",
+        [&]() { return EvaluateAbcFile(runtime, NG::GetSystemPath("jsEnumStyle.abc")); });
 }
 
 inline bool PreloadStateManagement(const shared_ptr<JsRuntime>& runtime)
 {
     // set __hasUIFramework__
     runtime->GetGlobal()->SetProperty(runtime, "__hasUIFramework__", runtime->NewBoolean(true));
+    return PreloadAbcOnce(runtime, "__stateMgmtLoaded__", [&]() {
 #ifdef STATE_MGMT_USE_AOT
-    return runtime->ExecuteJsBinForAOT("/etc/abc/framework/stateMgmt.abc");
+        return runtime->ExecuteJsBinForAOT("/etc/abc/framework/stateMgmt.abc");
 #else
-    std::string str("arkui_binary_stateMgmt_abc_loadFile");
-    return runtime->EvaluateJsCode(
-        reinterpret_cast<const uint8_t*>(_binary_stateMgmt_abc_start),
-        _binary_stateMgmt_abc_end - _binary_stateMgmt_abc_start, str);
+        std::string str("arkui_binary_stateMgmt_abc_loadFile");
+        return runtime->EvaluateJsCode(
+            reinterpret_cast<const uint8_t*>(_binary_stateMgmt_abc_start),
+            _binary_stateMgmt_abc_end - _binary_stateMgmt_abc_start, str);
 #endif
+    });
 }
 
 inline bool PreloadUIContent(const shared_ptr<JsRuntime>& runtime)
 {
-    uint8_t* codeStart = const_cast<uint8_t*>(reinterpret_cast<const uint8_t*>(_binary_jsPreload_abc_start));
-    int32_t codeLength = _binary_jsPreload_abc_end - _binary_jsPreload_abc_start;
-    return runtime->EvaluateJsCode(codeStart, codeLength);
+    return PreloadAbcOnce(runtime, "__uiContentPreloaded__", [&]() {
+        uint8_t* codeStart = const_cast<uint8_t*>(reinterpret_cast<const uint8_t*>(_binary_jsPreload_abc_start));
+        int32_t codeLength = _binary_jsPreload_abc_end - _binary_jsPreload_abc_start;
+        return runtime->EvaluateJsCode(codeStart, codeLength);
+    });
 }
 
 inline bool PreloadArkCommon(const shared_ptr<JsRuntime>& runtime)
 {
-    std::string str("arkui_binary_arkCommon_abc_loadFile");
-    return runtime->EvaluateJsCode(
-        (uint8_t*)_binary_arkCommon_abc_start, _binary_arkCommon_abc_end - _binary_arkCommon_abc_start, str);
+    // arkCommon.abc has a top-level `const arkUINativeModule`; double-execution is the case
+    // that surfaced the "Duplicate identifier" crash. See PreloadAbcOnce.
+    return PreloadAbcOnce(runtime, "__arkCommonLoaded__", [&]() {
+        std::string str("arkui_binary_arkCommon_abc_loadFile");
+        return runtime->EvaluateJsCode(
+            (uint8_t*)_binary_arkCommon_abc_start, _binary_arkCommon_abc_end - _binary_arkCommon_abc_start, str);
+    });
 }
 
 inline bool PreloadArkDynamicComponent(const shared_ptr<JsRuntime>& runtime)
 {
-    std::string str("arkui_binary_arkDynamicComponent_abc_loadFile");
-    return runtime->EvaluateJsCode((uint8_t*)_binary_arkDynamicComponent_abc_start,
-        _binary_arkDynamicComponent_abc_end - _binary_arkDynamicComponent_abc_start, str);
+    return PreloadAbcOnce(runtime, "__arkDynamicComponentLoaded__", [&]() {
+        std::string str("arkui_binary_arkDynamicComponent_abc_loadFile");
+        return runtime->EvaluateJsCode((uint8_t*)_binary_arkDynamicComponent_abc_start,
+            _binary_arkDynamicComponent_abc_end - _binary_arkDynamicComponent_abc_start, str);
+    });
 }
 
 inline bool PreloadArkComponent(const shared_ptr<JsRuntime>& runtime)
 {
-    return EvaluateAbcFile(runtime, NG::GetSystemPath("arkComponent.abc"));
+    return PreloadAbcOnce(runtime, "__arkComponentLoaded__",
+        [&]() { return EvaluateAbcFile(runtime, NG::GetSystemPath("arkComponent.abc")); });
 }
 
 bool PreloadConsole(const shared_ptr<JsRuntime>& runtime, const shared_ptr<JsValue>& global)
@@ -1123,6 +1161,13 @@ void JsiDeclarativeEngineInstance::DestroyAllRootViewHandle()
 void JsiDeclarativeEngineInstance::FlushReload()
 {
     CHECK_RUN_ON(JS);
+    // Guard before JAVASCRIPT_EXECUTION_SCOPE_STATIC, which dereferences
+    // GetCurrentRuntime()->GetEcmaVm(). A configuration change (e.g. color mode)
+    // broadcasts UpdateConfiguration -> SetColor -> FlushReload to every container,
+    // including a sub-window (Dialog/Menu/Popup) container that has no current JS
+    // runtime on this thread, so GetCurrentRuntime() is null and the macro would
+    // crash (EXC_BAD_ACCESS in GetEcmaVm on a null this).
+    CHECK_JAVASCRIPT_SCOPE_AND_RETURN;
     JAVASCRIPT_EXECUTION_SCOPE_STATIC;
     if (rootViewMap_.empty()) {
         return;
@@ -2145,6 +2190,12 @@ void JsiDeclarativeEngine::LoadJsWithModule(
     const std::string assetPath = ASSET_PATH_PREFIX + moduleName + "/" + FORM_ES_MODULE_PATH;
     runtime->SetAssetPath(assetPath);
     runtime->SetModuleName(moduleName);
+#ifdef CROSS_PLATFORM
+    // The page is an esmodule loaded from the in-memory modules.abc. The shared VM may
+    // still be in bundle-pack mode (default), which would make JSNApi::Execute treat the
+    // page url as a script file path and fail. Force esmodule resolution for the page.
+    runtime->SetBundle(false);
+#endif
     if (urlName.substr(0, strlen(BUNDLE_TAG)) != BUNDLE_TAG) {
 #ifdef CROSS_PLATFORM
         std::string moduleNamePath = (moduleName.find_last_of('.') != std::string::npos)
@@ -2155,7 +2206,7 @@ void JsiDeclarativeEngine::LoadJsWithModule(
         urlName = container->GetModuleName() + "/ets/" + urlName;
 #endif
     }
-    runtime->ExecuteJsBin(urlName, errorCallback);
+    bool r = runtime->ExecuteJsBin(urlName, errorCallback);
 }
 #endif
 // Load the app.js file of the FA model in NG structure.
diff --git a/frameworks/bridge/declarative_frontend/ng/page_router_manager.cpp b/frameworks/bridge/declarative_frontend/ng/page_router_manager.cpp
index a40c3c217e2..30c68979098 100644
--- a/frameworks/bridge/declarative_frontend/ng/page_router_manager.cpp
+++ b/frameworks/bridge/declarative_frontend/ng/page_router_manager.cpp
@@ -2667,11 +2667,13 @@ bool PageRouterManager::GenerateRouterPageInner(const RouterPageInfo& target)
         auto intentInfo = target.intentInfo;
         return generateIntentPageCallback_(intentInfo.bundleName, intentInfo.moduleName, intentInfo.pagePath);
     }
-    if (loadNamedRouter_(target.url, target.isNamedRouterMode)) {
+    bool lnr = loadNamedRouter_(target.url, target.isNamedRouterMode);
+    if (lnr) {
         return true;
     }
     if (!target.isNamedRouterMode) {
-        return updateRootComponent_();
+        bool urc = updateRootComponent_();
+        return urc;
     }
     if (target.errorCallback) {
         target.errorCallback("The named route is not exist.", ERROR_CODE_NAMED_ROUTE_ERROR);
diff --git a/frameworks/bridge/declarative_frontend/state_mgmt/package-lock.json b/frameworks/bridge/declarative_frontend/state_mgmt/package-lock.json
index 303990672eb..241e87f0aa6 100644
--- a/frameworks/bridge/declarative_frontend/state_mgmt/package-lock.json
+++ b/frameworks/bridge/declarative_frontend/state_mgmt/package-lock.json
@@ -1,31 +1,67 @@
 {
   "name": "statemgmt",
   "version": "0.0.1",
-  "lockfileVersion": 1,
+  "lockfileVersion": 3,
   "requires": true,
-  "dependencies": {
-    "@types/node": {
+  "packages": {
+    "": {
+      "name": "statemgmt",
+      "version": "0.0.1",
+      "license": "Apache-2.0",
+      "dependencies": {
+        "pegjs": "^0.10.0"
+      },
+      "devDependencies": {
+        "@types/node": "^15.0.1",
+        "json5": "^2.2.3",
+        "typescript": "^4.9.5"
+      }
+    },
+    "node_modules/@types/node": {
       "version": "15.14.9",
       "resolved": "https://repo.huaweicloud.com/repository/npm/@types/node/-/node-15.14.9.tgz",
       "integrity": "sha512-qjd88DrCxupx/kJD5yQgZdcYKZKSIGBVDIBE1/LTGcNm3d2Np/jxojkdePDdfnBHJc5W7vSMpbJ1aB7p/Py69A==",
-      "dev": true
+      "dev": true,
+      "license": "MIT"
     },
-    "json5": {
+    "node_modules/json5": {
       "version": "2.2.3",
       "resolved": "https://repo.huaweicloud.com/repository/npm/json5/-/json5-2.2.3.tgz",
       "integrity": "sha512-XmOWe7eyHYH14cLdVPoyg+GOH3rYX++KpzrylJwSW98t3Nk+U8XOl8FWKOgwtzdb8lXGf6zYwDUzeHMWfxasyg==",
-      "dev": true
+      "dev": true,
+      "license": "MIT",
+      "bin": {
+        "json5": "lib/cli.js"
+      },
+      "engines": {
+        "node": ">=6"
+      }
     },
-    "pegjs": {
+    "node_modules/pegjs": {
       "version": "0.10.0",
       "resolved": "https://repo.huaweicloud.com/repository/npm/pegjs/-/pegjs-0.10.0.tgz",
-      "integrity": "sha512-qI5+oFNEGi3L5HAxDwN2LA4Gg7irF70Zs25edhjld9QemOgp0CbvMtbFcMvFtEo1OityPrcCzkQFB8JP/hxgow=="
+      "integrity": "sha512-qI5+oFNEGi3L5HAxDwN2LA4Gg7irF70Zs25edhjld9QemOgp0CbvMtbFcMvFtEo1OityPrcCzkQFB8JP/hxgow==",
+      "license": "MIT",
+      "bin": {
+        "pegjs": "bin/pegjs"
+      },
+      "engines": {
+        "node": ">=0.10"
+      }
     },
-    "typescript": {
+    "node_modules/typescript": {
       "version": "4.9.5",
       "resolved": "https://repo.huaweicloud.com/repository/npm/typescript/-/typescript-4.9.5.tgz",
       "integrity": "sha512-1FXk9E2Hm+QzZQ7z+McJiHL4NW1F2EzMu9Nq9i3zAaGqibafqYwCVU6WyWAuyQRRzOlxou8xZSyXLEN8oKj24g==",
-      "dev": true
+      "dev": true,
+      "license": "Apache-2.0",
+      "bin": {
+        "tsc": "bin/tsc",
+        "tsserver": "bin/tsserver"
+      },
+      "engines": {
+        "node": ">=4.2.0"
+      }
     }
   }
 }
diff --git a/frameworks/bridge/declarative_frontend/state_mgmt/src/lib/puv2_common/puv2_view_base.ts b/frameworks/bridge/declarative_frontend/state_mgmt/src/lib/puv2_common/puv2_view_base.ts
index 29686e11b15..51f7dfc1536 100644
--- a/frameworks/bridge/declarative_frontend/state_mgmt/src/lib/puv2_common/puv2_view_base.ts
+++ b/frameworks/bridge/declarative_frontend/state_mgmt/src/lib/puv2_common/puv2_view_base.ts
@@ -1334,6 +1334,15 @@ abstract class PUV2ViewBase extends ViewBuildNodeBase {
   }
 
   public static isNeedBuildPrebuildCmd(): boolean {
+    // ArkUI-X macOS: in the dynamic-render shared-runtime scenario the stateMgmt
+    // script-global `PrebuildPhase` enum may be absent from the page execution
+    // context (PUV2ViewBase is registered via JsRegisterViews but the top-level
+    // var is not on this realm's global). Prebuild is disabled on macOS anyway
+    // (system_properties prebuildInMultiFrameEnabled_=false), so guard the lookup
+    // to keep observeComponentCreation2 from throwing and aborting the first frame.
+    if (typeof PrebuildPhase === 'undefined') {
+      return false;
+    }
     const needBuild: boolean = PUV2ViewBase.prebuildPhase_ === PrebuildPhase.BuildPrebuildCmd;
     return needBuild;
   }
diff --git a/frameworks/core/common/ime/input_method_manager.h b/frameworks/core/common/ime/input_method_manager.h
index 55b8cefe9e4..03fc6ef9a85 100644
--- a/frameworks/core/common/ime/input_method_manager.h
+++ b/frameworks/core/common/ime/input_method_manager.h
@@ -56,6 +56,11 @@ public:
     void Attach(const WeakPtr<TextInputClient>& client, const TextInputConfiguration& config,
         const RefPtr<TaskExecutor>& taskExecutor, int32_t instanceId);
     void FinishComposing(int32_t instanceId);
+#if defined(MAC_PLATFORM)
+    // Push the focused editable's caret rect (ArkUI window pixels, top-left origin)
+    // to the platform IME bridge so the candidate window anchors at the caret.
+    void UpdateCursorInfo(double left, double top, double width, double height);
+#endif
 #endif
 private:
     InputMethodManager() = default;
diff --git a/frameworks/core/components_ng/layout/utils.h b/frameworks/core/components_ng/layout/utils.h
index 02ec84ad704..6be91ef9eef 100644
--- a/frameworks/core/components_ng/layout/utils.h
+++ b/frameworks/core/components_ng/layout/utils.h
@@ -21,7 +21,10 @@
 #include "core/components/common/properties/alignment.h"
 
 namespace OHOS::Ace::NG {
-Alignment ConvertStringToAlignment(std::string localizedAlignment)
+// Header-defined function must be `inline`, else every TU including this header
+// emits a strong definition → duplicate symbol when statically linking the macOS
+// single binary (iOS avoids this via per-component dylibs). `inline` coalesces them.
+inline Alignment ConvertStringToAlignment(std::string localizedAlignment)
 {
     static const std::unordered_map<std::string, Alignment> alignmentMap = {
         {"top_start", Alignment::TOP_LEFT}, {"top", Alignment::TOP_CENTER}, {"top_end", Alignment::TOP_RIGHT},
diff --git a/frameworks/core/components_ng/pattern/calendar_picker/bridge/calendar_picker_dialog_dynamic_modifier.cpp b/frameworks/core/components_ng/pattern/calendar_picker/bridge/calendar_picker_dialog_dynamic_modifier.cpp
index c3f37e59461..3b82bdb341a 100644
--- a/frameworks/core/components_ng/pattern/calendar_picker/bridge/calendar_picker_dialog_dynamic_modifier.cpp
+++ b/frameworks/core/components_ng/pattern/calendar_picker/bridge/calendar_picker_dialog_dynamic_modifier.cpp
@@ -94,7 +94,7 @@ const ArkUICalendarPickerDialogModifier* GetCalendarPickerDialogDynamicModifier(
     return &modifier;
 }
 
-const CJUICalendarPickerDialogModifier* GetCJUICalendarPickerDialogModifier()
+const CJUICalendarPickerDialogModifier* GetCJUICalendarPickerDialogModifierStatic()
 {
     CHECK_INITIALIZED_FIELDS_BEGIN(); // don't move this line
     static const CJUICalendarPickerDialogModifier modifier = {
diff --git a/frameworks/core/components_ng/pattern/calendar_picker/bridge/calendar_picker_dialog_dynamic_module.cpp b/frameworks/core/components_ng/pattern/calendar_picker/bridge/calendar_picker_dialog_dynamic_module.cpp
index a3e7dde75ce..253b7d10a71 100644
--- a/frameworks/core/components_ng/pattern/calendar_picker/bridge/calendar_picker_dialog_dynamic_module.cpp
+++ b/frameworks/core/components_ng/pattern/calendar_picker/bridge/calendar_picker_dialog_dynamic_module.cpp
@@ -28,7 +28,7 @@ namespace OHOS::Ace {
 
 namespace NG {
 const ArkUICalendarPickerDialogModifier* GetCalendarPickerDialogDynamicModifier();
-const CJUICalendarPickerDialogModifier* GetCJUICalendarPickerDialogModifier();
+const CJUICalendarPickerDialogModifier* GetCJUICalendarPickerDialogModifierStatic();
 #ifdef INCLUDE_GENERATED_SOURCES
 namespace GeneratedModifier {
 const GENERATED_ArkUICalendarPickerDialogAccessor* GetCalendarPickerDialogAccessor();
@@ -58,6 +58,6 @@ const void* CalendarPickerDialogDynamicModule::GetStaticModifier()
 
 const void* CalendarPickerDialogDynamicModule::GetCjModifier()
 {
-    return NG::GetCJUICalendarPickerDialogModifier();
+    return NG::GetCJUICalendarPickerDialogModifierStatic();
 }
 } // namespace OHOS::Ace
\ No newline at end of file
diff --git a/frameworks/core/components_ng/pattern/calendar_picker/bridge/calendar_picker_dynamic_modifier.cpp b/frameworks/core/components_ng/pattern/calendar_picker/bridge/calendar_picker_dynamic_modifier.cpp
index aefe5fbf4ef..5e91418557e 100644
--- a/frameworks/core/components_ng/pattern/calendar_picker/bridge/calendar_picker_dynamic_modifier.cpp
+++ b/frameworks/core/components_ng/pattern/calendar_picker/bridge/calendar_picker_dynamic_modifier.cpp
@@ -1436,7 +1436,7 @@ const ArkUICalendarPickerModifier* GetCalendarPickerDynamicModifier()
     }
 }
 
-const CJUICalendarPickerModifier* GetCJUICalendarPickerModifier()
+const CJUICalendarPickerModifier* GetCJUICalendarPickerModifierStatic()
 {
     CHECK_INITIALIZED_FIELDS_BEGIN(); // don't move this line
     static const CJUICalendarPickerModifier modifier = {
diff --git a/frameworks/core/components_ng/pattern/calendar_picker/bridge/calendar_picker_dynamic_module.cpp b/frameworks/core/components_ng/pattern/calendar_picker/bridge/calendar_picker_dynamic_module.cpp
index bad44e180c4..87c6159f46a 100644
--- a/frameworks/core/components_ng/pattern/calendar_picker/bridge/calendar_picker_dynamic_module.cpp
+++ b/frameworks/core/components_ng/pattern/calendar_picker/bridge/calendar_picker_dynamic_module.cpp
@@ -32,7 +32,7 @@ namespace OHOS::Ace {
 
 namespace NG {
 const ArkUICalendarPickerModifier* GetCalendarPickerDynamicModifier();
-const CJUICalendarPickerModifier* GetCJUICalendarPickerModifier();
+const CJUICalendarPickerModifier* GetCJUICalendarPickerModifierStatic();
 #ifdef INCLUDE_GENERATED_SOURCES
 namespace GeneratedModifier {
 const GENERATED_ArkUICalendarPickerModifier* GetCalendarPickerStaticModifier();
@@ -62,7 +62,7 @@ const void* CalendarPickerDynamicModule::GetStaticModifier()
 
 const void* CalendarPickerDynamicModule::GetCjModifier()
 {
-    return NG::GetCJUICalendarPickerModifier();
+    return NG::GetCJUICalendarPickerModifierStatic();
 }
 
 void* CalendarPickerDynamicModule::GetModel()
diff --git a/frameworks/core/components_ng/pattern/counter/bridge/counter_dynamic_modifier.cpp b/frameworks/core/components_ng/pattern/counter/bridge/counter_dynamic_modifier.cpp
index afef4922b2c..11b3640d1dd 100644
--- a/frameworks/core/components_ng/pattern/counter/bridge/counter_dynamic_modifier.cpp
+++ b/frameworks/core/components_ng/pattern/counter/bridge/counter_dynamic_modifier.cpp
@@ -173,7 +173,7 @@ void SetCounterBackgroundColorRes(ArkUINodeHandle node, ArkUI_Uint32 color, ArkU
     }
 }
 
-void SetBackgroundColorForHDR(
+static void SetBackgroundColorForHDR(
     ArkUINodeHandle node, ArkUI_Int32 colorSpace, const ArkUI_Float32* hdrValues, void* colorRawPtr)
 {
     FrameNode* frameNode = GetFrameNode(node);
@@ -249,7 +249,7 @@ void ResetCounterOnDec(ArkUINodeHandle node)
     CounterModelNG::SetOnDec(frameNode, nullptr);
 }
 
-ArkUINodeHandle CreateFrameNode(int32_t nodeId)
+static ArkUINodeHandle CreateFrameNode(int32_t nodeId)
 {
     return nullptr;
 }
diff --git a/frameworks/core/components_ng/pattern/data_panel/bridge/data_panel_dynamic_modifier.cpp b/frameworks/core/components_ng/pattern/data_panel/bridge/data_panel_dynamic_modifier.cpp
index 08e5148d8cc..0aaaa96ee3a 100644
--- a/frameworks/core/components_ng/pattern/data_panel/bridge/data_panel_dynamic_modifier.cpp
+++ b/frameworks/core/components_ng/pattern/data_panel/bridge/data_panel_dynamic_modifier.cpp
@@ -850,7 +850,7 @@ void SetDataPanelStrokeWidthNewImpl(
 {}
 #endif
 
-ArkUINodeHandle CreateFrameNode(ArkUI_Int32 nodeId)
+static ArkUINodeHandle CreateFrameNode(ArkUI_Int32 nodeId)
 {
     return nullptr;
 }
diff --git a/frameworks/core/components_ng/pattern/gauge/bridge/gauge_dynamic_modifier.cpp b/frameworks/core/components_ng/pattern/gauge/bridge/gauge_dynamic_modifier.cpp
index b06ed9fc164..0d75c84ec31 100644
--- a/frameworks/core/components_ng/pattern/gauge/bridge/gauge_dynamic_modifier.cpp
+++ b/frameworks/core/components_ng/pattern/gauge/bridge/gauge_dynamic_modifier.cpp
@@ -725,7 +725,9 @@ void SetIsShowDescriptionImpl(ArkUINodeHandle node, ArkUI_Bool isShowDescription
 void SetDescriptionImpl() {}
 #endif
 
-ArkUINodeHandle CreateFrameNode(int32_t nodeId)
+// ArkUI-X macOS: internal linkage to avoid colliding with other components' identically-named
+// helper when statically linked into the single ace_macos binary (iOS = per-component dylib).
+static ArkUINodeHandle CreateFrameNode(int32_t nodeId)
 {
     return nullptr;
 }
diff --git a/frameworks/core/components_ng/pattern/indexer/bridge/indexer_dynamic_modifier.cpp b/frameworks/core/components_ng/pattern/indexer/bridge/indexer_dynamic_modifier.cpp
index 83bdda8c2fb..181bb8a8b38 100644
--- a/frameworks/core/components_ng/pattern/indexer/bridge/indexer_dynamic_modifier.cpp
+++ b/frameworks/core/components_ng/pattern/indexer/bridge/indexer_dynamic_modifier.cpp
@@ -295,7 +295,7 @@ void SetSelectedBackgroundColor(ArkUINodeHandle node, uint32_t color)
     IndexerModelNG::SetSelectedBGColorByUser(frameNode, true);
 }
 
-void ResetSelectedBackgroundColor(ArkUINodeHandle node)
+static void ResetSelectedBackgroundColor(ArkUINodeHandle node)
 {
     auto* frameNode = GetFrameNode(node);
     CHECK_NULL_VOID(frameNode);
@@ -574,14 +574,14 @@ void ResetAutoCollapse(ArkUINodeHandle node)
     IndexerModelNG::SetAutoCollapse(frameNode, true);
 }
 
-void SetEnableHapticFeedback(ArkUINodeHandle node, ArkUI_Bool value)
+static void SetEnableHapticFeedback(ArkUINodeHandle node, ArkUI_Bool value)
 {
     auto* frameNode = GetFrameNode(node);
     CHECK_NULL_VOID(frameNode);
     IndexerModelNG::SetEnableHapticFeedback(frameNode, value);
 }
 
-void ResetEnableHapticFeedback(ArkUINodeHandle node)
+static void ResetEnableHapticFeedback(ArkUINodeHandle node)
 {
     auto* frameNode = GetFrameNode(node);
     CHECK_NULL_VOID(frameNode);
diff --git a/frameworks/core/components_ng/pattern/linear_split/bridge/column_split_dynamic_modifier.cpp b/frameworks/core/components_ng/pattern/linear_split/bridge/column_split_dynamic_modifier.cpp
index 7df56b3bb30..ff794ec99c1 100644
--- a/frameworks/core/components_ng/pattern/linear_split/bridge/column_split_dynamic_modifier.cpp
+++ b/frameworks/core/components_ng/pattern/linear_split/bridge/column_split_dynamic_modifier.cpp
@@ -197,7 +197,7 @@ const ArkUIColumnSplitModifier* GetColumnSplitDynamicModifier()
     return &modifier;
 }
 
-const CJUIColumnSplitModifier* GetCJUIColumnSplitModifier()
+const CJUIColumnSplitModifier* GetCJUIColumnSplitModifierStatic()
 {
     CHECK_INITIALIZED_FIELDS_BEGIN(); // don't move this line
     static const CJUIColumnSplitModifier modifier = {
diff --git a/frameworks/core/components_ng/pattern/linear_split/bridge/column_split_dynamic_module.cpp b/frameworks/core/components_ng/pattern/linear_split/bridge/column_split_dynamic_module.cpp
index 3de02b9bddd..a906313b85c 100644
--- a/frameworks/core/components_ng/pattern/linear_split/bridge/column_split_dynamic_module.cpp
+++ b/frameworks/core/components_ng/pattern/linear_split/bridge/column_split_dynamic_module.cpp
@@ -33,7 +33,7 @@ namespace OHOS::Ace {
 namespace NG {
 namespace NodeModifier {
 const ArkUIColumnSplitModifier* GetColumnSplitDynamicModifier();
-const CJUIColumnSplitModifier* GetCJUIColumnSplitModifier();
+const CJUIColumnSplitModifier* GetCJUIColumnSplitModifierStatic();
 } // namespace NodeModifier
 #ifdef INCLUDE_GENERATED_SOURCES
 namespace GeneratedModifier {
@@ -63,7 +63,7 @@ const void* ColumnSplitDynamicModule::GetStaticModifier()
 
 const void* ColumnSplitDynamicModule::GetCjModifier()
 {
-    return NG::NodeModifier::GetCJUIColumnSplitModifier();
+    return NG::NodeModifier::GetCJUIColumnSplitModifierStatic();
 }
 
 void* ColumnSplitDynamicModule::GetModel()
diff --git a/frameworks/core/components_ng/pattern/linear_split/bridge/row_split_dynamic_modifier.cpp b/frameworks/core/components_ng/pattern/linear_split/bridge/row_split_dynamic_modifier.cpp
index 4680025309d..32a4608e151 100644
--- a/frameworks/core/components_ng/pattern/linear_split/bridge/row_split_dynamic_modifier.cpp
+++ b/frameworks/core/components_ng/pattern/linear_split/bridge/row_split_dynamic_modifier.cpp
@@ -144,7 +144,7 @@ const ArkUIRowSplitModifier* GetRowSplitDynamicModifier()
     return &modifier;
 }
 
-const CJUIRowSplitModifier* GetCJUIRowSplitModifier()
+const CJUIRowSplitModifier* GetCJUIRowSplitModifierStatic()
 {
     CHECK_INITIALIZED_FIELDS_BEGIN(); // don't move this line
     static const CJUIRowSplitModifier modifier = {
diff --git a/frameworks/core/components_ng/pattern/linear_split/bridge/row_split_dynamic_module.cpp b/frameworks/core/components_ng/pattern/linear_split/bridge/row_split_dynamic_module.cpp
index 71d56179a61..344f80d7147 100644
--- a/frameworks/core/components_ng/pattern/linear_split/bridge/row_split_dynamic_module.cpp
+++ b/frameworks/core/components_ng/pattern/linear_split/bridge/row_split_dynamic_module.cpp
@@ -32,7 +32,7 @@ namespace OHOS::Ace {
 namespace NG {
 namespace NodeModifier {
 const ArkUIRowSplitModifier* GetRowSplitDynamicModifier();
-const CJUIRowSplitModifier* GetCJUIRowSplitModifier();
+const CJUIRowSplitModifier* GetCJUIRowSplitModifierStatic();
 } // namespace NodeModifier
 #ifdef INCLUDE_GENERATED_SOURCES
 namespace GeneratedModifier {
@@ -62,7 +62,7 @@ const void* RowSplitDynamicModule::GetStaticModifier()
 
 const void* RowSplitDynamicModule::GetCjModifier()
 {
-    return NG::NodeModifier::GetCJUIRowSplitModifier();
+    return NG::NodeModifier::GetCJUIRowSplitModifierStatic();
 }
 
 void* RowSplitDynamicModule::GetModel()
diff --git a/frameworks/core/components_ng/pattern/marquee/bridge/marquee_dynamic_modifier.cpp b/frameworks/core/components_ng/pattern/marquee/bridge/marquee_dynamic_modifier.cpp
index 531748c35a9..44af663629f 100644
--- a/frameworks/core/components_ng/pattern/marquee/bridge/marquee_dynamic_modifier.cpp
+++ b/frameworks/core/components_ng/pattern/marquee/bridge/marquee_dynamic_modifier.cpp
@@ -720,7 +720,7 @@ const ArkUIMarqueeModifier* GetMarqueeDynamicModifier()
     }
 }
 
-const CJUIMarqueeModifier* GetCJUIMarqueeModifier()
+const CJUIMarqueeModifier* GetCJUIMarqueeModifierStatic()
 {
     CHECK_INITIALIZED_FIELDS_BEGIN(); // don't move this line
     static const CJUIMarqueeModifier modifier = {
diff --git a/frameworks/core/components_ng/pattern/marquee/bridge/marquee_dynamic_module.cpp b/frameworks/core/components_ng/pattern/marquee/bridge/marquee_dynamic_module.cpp
index c53196d35f9..8c995f907ed 100644
--- a/frameworks/core/components_ng/pattern/marquee/bridge/marquee_dynamic_module.cpp
+++ b/frameworks/core/components_ng/pattern/marquee/bridge/marquee_dynamic_module.cpp
@@ -35,7 +35,7 @@ namespace OHOS::Ace {
 namespace NG {
 namespace NodeModifier {
 const ArkUIMarqueeModifier* GetMarqueeDynamicModifier();
-const CJUIMarqueeModifier* GetCJUIMarqueeModifier();
+const CJUIMarqueeModifier* GetCJUIMarqueeModifierStatic();
 }
 namespace CustomModifier {
     constexpr const char* CUSTOM_MODIFIER = "customModifier";
@@ -69,7 +69,7 @@ const void* MarqueeDynamicModule::GetStaticModifier()
 
 const void* MarqueeDynamicModule::GetCjModifier()
 {
-    return NG::NodeModifier::GetCJUIMarqueeModifier();
+    return NG::NodeModifier::GetCJUIMarqueeModifierStatic();
 }
 
 const void* MarqueeDynamicModule::GetCustomModifier(const std::string& name)
diff --git a/frameworks/core/components_ng/pattern/patternlock/bridge/patternlock_dynamic_modifier.cpp b/frameworks/core/components_ng/pattern/patternlock/bridge/patternlock_dynamic_modifier.cpp
index 7157de17677..6d55e85cce5 100644
--- a/frameworks/core/components_ng/pattern/patternlock/bridge/patternlock_dynamic_modifier.cpp
+++ b/frameworks/core/components_ng/pattern/patternlock/bridge/patternlock_dynamic_modifier.cpp
@@ -37,12 +37,12 @@ FrameNode* GetFrameNode(ArkUINodeHandle node)
     return node ? reinterpret_cast<FrameNode*>(node) : ViewStackProcessor::GetInstance()->GetMainFrameNode();
 }
 
-ArkUINodeHandle CreateFrameNode(int32_t nodeId)
+static ArkUINodeHandle CreateFrameNode(int32_t nodeId)
 {
     return nullptr;
 }
 
-void CreateModel(ArkUI_Bool isObject, void* controller)
+static void CreateModel(ArkUI_Bool isObject, void* controller)
 {
     static PatternLockModelNG model;
     auto controllers = model.Create();
diff --git a/frameworks/core/components_ng/pattern/rating/bridge/arkts_native_rating_bridge.cpp b/frameworks/core/components_ng/pattern/rating/bridge/arkts_native_rating_bridge.cpp
index f346de52885..d8c95a56d87 100644
--- a/frameworks/core/components_ng/pattern/rating/bridge/arkts_native_rating_bridge.cpp
+++ b/frameworks/core/components_ng/pattern/rating/bridge/arkts_native_rating_bridge.cpp
@@ -55,7 +55,9 @@ panda::Local<panda::JSValueRef> JsRatingChangeCallback(panda::JsiRuntimeCallInfo
     return panda::JSValueRef::Undefined(vm);
 }
 
-bool GetNativeNode(ArkUINodeHandle& nativeNode, const Local<JSValueRef>& firstArg, panda::ecmascript::EcmaVM* vm)
+// ArkUI-X macOS: internal linkage to avoid colliding with the identically-named helper in
+// other components' bridges under the single static ace_macos binary (iOS = per-component dylib).
+static bool GetNativeNode(ArkUINodeHandle& nativeNode, const Local<JSValueRef>& firstArg, panda::ecmascript::EcmaVM* vm)
 {
     if (firstArg->IsNativePointer(vm)) {
         nativeNode = nodePtr(firstArg->ToNativePointer(vm)->Value());
@@ -68,7 +70,7 @@ bool GetNativeNode(ArkUINodeHandle& nativeNode, const Local<JSValueRef>& firstAr
     return false;
 }
 
-bool IsJsView(const Local<JSValueRef>& firstArg, panda::ecmascript::EcmaVM* vm)
+static bool IsJsView(const Local<JSValueRef>& firstArg, panda::ecmascript::EcmaVM* vm)
 {
     return firstArg->IsBoolean() && firstArg->ToBoolean(vm)->Value();
 }
diff --git a/frameworks/core/components_ng/pattern/rating/bridge/rating_dynamic_modifier.cpp b/frameworks/core/components_ng/pattern/rating/bridge/rating_dynamic_modifier.cpp
index 83289658b57..16a6541d418 100644
--- a/frameworks/core/components_ng/pattern/rating/bridge/rating_dynamic_modifier.cpp
+++ b/frameworks/core/components_ng/pattern/rating/bridge/rating_dynamic_modifier.cpp
@@ -175,7 +175,9 @@ void ResetOnChange(ArkUINodeHandle node)
     RatingModelNG::SetOnChange(frameNode, nullptr);
 }
 
-ArkUINodeHandle CreateFrameNode(int32_t nodeId)
+// ArkUI-X macOS: internal linkage to avoid colliding with other components' identically-named
+// helper when statically linked into the single ace_macos binary (iOS = per-component dylib).
+static ArkUINodeHandle CreateFrameNode(int32_t nodeId)
 {
     auto frameNode = RatingModelNG::CreateFrameNode(nodeId);
     CHECK_NULL_RETURN(frameNode, nullptr);
@@ -190,7 +192,10 @@ void CreateRating(ArkUI_Float64 rating, ArkUI_Bool indicator)
     RatingModelNG::CreateRating(rating, static_cast<bool>(indicator));
 }
 
-void SetOnChangeEvent(void* callback)
+// ArkUI-X macOS: internal linkage so this per-component helper does not collide with the
+// identically-named NG::SetOnChangeEvent in other components' bridges when they are all
+// statically linked into the single ace_macos binary (iOS keeps each in its own dylib).
+static void SetOnChangeEvent(void* callback)
 {
     if (callback) {
         auto onChange = reinterpret_cast<std::function<void(const std::string&)>*>(callback);
diff --git a/frameworks/core/components_ng/pattern/rich_editor/bridge/rich_editor_dynamic_modifier.cpp b/frameworks/core/components_ng/pattern/rich_editor/bridge/rich_editor_dynamic_modifier.cpp
index bae6626a26b..a74e3c166b0 100644
--- a/frameworks/core/components_ng/pattern/rich_editor/bridge/rich_editor_dynamic_modifier.cpp
+++ b/frameworks/core/components_ng/pattern/rich_editor/bridge/rich_editor_dynamic_modifier.cpp
@@ -2246,7 +2246,7 @@ const ArkUIRichEditorModifier* GetRichEditorDynamicModifier()
     return &modifier;
 }
 
-const CJUIRichEditorModifier* GetCJUIRichEditorDynamicModifier()
+const CJUIRichEditorModifier* GetCJUIRichEditorDynamicModifierStatic()
 {
     CHECK_INITIALIZED_FIELDS_BEGIN(); // don't move this line
     static const CJUIRichEditorModifier modifier = {
diff --git a/frameworks/core/components_ng/pattern/rich_editor/bridge/rich_editor_dynamic_module.cpp b/frameworks/core/components_ng/pattern/rich_editor/bridge/rich_editor_dynamic_module.cpp
index 6fd0b786ec4..315bcdeb8ea 100644
--- a/frameworks/core/components_ng/pattern/rich_editor/bridge/rich_editor_dynamic_module.cpp
+++ b/frameworks/core/components_ng/pattern/rich_editor/bridge/rich_editor_dynamic_module.cpp
@@ -34,7 +34,7 @@ namespace OHOS::Ace {
 namespace NG {
 namespace NodeModifier {
 const ArkUIRichEditorModifier* GetRichEditorDynamicModifier();
-const CJUIRichEditorModifier* GetCJUIRichEditorDynamicModifier();
+const CJUIRichEditorModifier* GetCJUIRichEditorDynamicModifierStatic();
 }
 
 #ifdef INCLUDE_GENERATED_SOURCES
@@ -65,7 +65,7 @@ const void* RichEditorDynamicModule::GetStaticModifier()
 
 const void* RichEditorDynamicModule::GetCjModifier()
 {
-    return NG::NodeModifier::GetCJUIRichEditorDynamicModifier();
+    return NG::NodeModifier::GetCJUIRichEditorDynamicModifierStatic();
 }
 
 void* RichEditorDynamicModule::GetModel()
diff --git a/frameworks/core/components_ng/pattern/search/bridge/arkts_native_search_bridge.cpp b/frameworks/core/components_ng/pattern/search/bridge/arkts_native_search_bridge.cpp
index 6b94db10436..dac2292d2ea 100644
--- a/frameworks/core/components_ng/pattern/search/bridge/arkts_native_search_bridge.cpp
+++ b/frameworks/core/components_ng/pattern/search/bridge/arkts_native_search_bridge.cpp
@@ -52,8 +52,8 @@ constexpr char CHECK_REGEX_VALID[] = "__checkRegexValid__";
 } // namespace
 
 namespace OHOS::Ace {
-const std::vector<TextAlign> TEXT_ALIGNS = { TextAlign::START, TextAlign::CENTER, TextAlign::END, TextAlign::JUSTIFY,
-    TextAlign::LEFT, TextAlign::RIGHT };
+// TEXT_ALIGNS 的 canonical 定义在 common_text_constants.{h,cpp}(extern + 单一定义)。mac 静态
+// 单一二进制下 bridge 不能再定义一份(与 common_text_constants.o 重复符号),改用其 extern。
 
 SearchModel* GetSearchModel()
 {
@@ -83,7 +83,7 @@ const char SEARCH_FIELD_ETS_TAG[] = "SearchField";
 constexpr TextDecorationStyle DEFAULT_DECORATION_STYLE = TextDecorationStyle::SOLID;
 constexpr double DEFAULT_LINE_THICKNESS_SCALE = 1.0;
 
-Local<JSValueRef> JsPreventDefault(panda::JsiRuntimeCallInfo* info)
+static Local<JSValueRef> JsPreventDefault(panda::JsiRuntimeCallInfo* info)
 {
     Local<JSValueRef> thisObj = info->GetThisRef();
     auto eventInfo =
@@ -145,7 +145,7 @@ bool CheckRegexValid(const std::string& pattern)
 #endif
 }
 
-bool GetNativeNode(ArkUINodeHandle& nativeNode, const Local<JSValueRef>& firstArg, panda::ecmascript::EcmaVM* vm)
+static bool GetNativeNode(ArkUINodeHandle& nativeNode, const Local<JSValueRef>& firstArg, panda::ecmascript::EcmaVM* vm)
 {
     if (firstArg->IsNativePointer(vm)) {
         nativeNode = nodePtr(firstArg->ToNativePointer(vm)->Value());
diff --git a/frameworks/core/components_ng/pattern/search/bridge/search_dynamic_modifier.cpp b/frameworks/core/components_ng/pattern/search/bridge/search_dynamic_modifier.cpp
index 28b076bcfff..7411562cf4d 100644
--- a/frameworks/core/components_ng/pattern/search/bridge/search_dynamic_modifier.cpp
+++ b/frameworks/core/components_ng/pattern/search/bridge/search_dynamic_modifier.cpp
@@ -33,8 +33,8 @@
 #include "core/interfaces/native/node/search_modifier.h"
 
 namespace OHOS::Ace {
-const std::vector<TextDirection> TEXT_DIRECTIONS = { TextDirection::LTR, TextDirection::RTL, TextDirection::INHERIT,
-    TextDirection::AUTO };
+// TEXT_DIRECTIONS 的 canonical 定义在 common_text_constants.{h,cpp}。mac 静态单一二进制下删 bridge
+// 冗余定义(与 common_text_constants.o 重复符号),改用其 extern。
 }
 
 namespace OHOS::Ace::NG {
@@ -2593,7 +2593,7 @@ const ArkUISearchModifier* GetSearchDynamicModifier()
     return &modifier;
 }
 
-const CJUISearchModifier* GetCJUISearchModifier()
+const CJUISearchModifier* GetCJUISearchModifierStatic()
 {
     CHECK_INITIALIZED_FIELDS_BEGIN(); // don't move this line
     static const CJUISearchModifier modifier = {
@@ -2693,7 +2693,7 @@ const CJUISearchModifier* GetCJUISearchModifier()
     return &modifier;
 }
 
-const ArkUISearchCustomModifier* GetSearchCustomModifier()
+const ArkUISearchCustomModifier* GetSearchCustomModifierStatic()
 {
     CHECK_INITIALIZED_FIELDS_BEGIN(); // don't move this line
     static const ArkUISearchCustomModifier modifier = {
diff --git a/frameworks/core/components_ng/pattern/search/bridge/search_dynamic_module.cpp b/frameworks/core/components_ng/pattern/search/bridge/search_dynamic_module.cpp
index 5d0b8c53116..acaaa120fbb 100644
--- a/frameworks/core/components_ng/pattern/search/bridge/search_dynamic_module.cpp
+++ b/frameworks/core/components_ng/pattern/search/bridge/search_dynamic_module.cpp
@@ -36,8 +36,8 @@ namespace OHOS::Ace {
 namespace NG {
 namespace NodeModifier {
 const ArkUISearchModifier* GetSearchDynamicModifier();
-const CJUISearchModifier* GetCJUISearchModifier();
-const ArkUISearchCustomModifier* GetSearchCustomModifier();
+const CJUISearchModifier* GetCJUISearchModifierStatic();
+const ArkUISearchCustomModifier* GetSearchCustomModifierStatic();
 } // namespace NodeModifier
 #ifdef INCLUDE_GENERATED_SOURCES
 namespace GeneratedModifier {
@@ -67,7 +67,7 @@ const void* SearchDynamicModule::GetStaticModifier()
 
 const void* SearchDynamicModule::GetCjModifier()
 {
-    return NG::NodeModifier::GetCJUISearchModifier();
+    return NG::NodeModifier::GetCJUISearchModifierStatic();
 }
 
 void* SearchDynamicModule::GetModel()
@@ -78,6 +78,6 @@ void* SearchDynamicModule::GetModel()
 
 const void* SearchDynamicModule::GetCustomModifier(const std::string& name)
 {
-    return NG::NodeModifier::GetSearchCustomModifier();
+    return NG::NodeModifier::GetSearchCustomModifierStatic();
 }
 } // namespace OHOS::Ace
\ No newline at end of file
diff --git a/frameworks/core/components_ng/pattern/slider/bridge/slider_dynamic_modifier.cpp b/frameworks/core/components_ng/pattern/slider/bridge/slider_dynamic_modifier.cpp
index 40f842f1e90..ad1df897be1 100644
--- a/frameworks/core/components_ng/pattern/slider/bridge/slider_dynamic_modifier.cpp
+++ b/frameworks/core/components_ng/pattern/slider/bridge/slider_dynamic_modifier.cpp
@@ -1038,7 +1038,10 @@ void SliderPaintPropertyUpdateValue(ArkUINodeHandle node, ArkUI_Float32 value)
     sliderPaintProperty->UpdateValue(static_cast<float>(value));
 }
 
-void SetOnChangeEvent(void* callback)
+// ArkUI-X macOS: internal linkage so this per-component helper does not collide with the
+// identically-named NG::SetOnChangeEvent in other components' bridges when they are all
+// statically linked into the single ace_macos binary (iOS keeps each in its own dylib).
+static void SetOnChangeEvent(void* callback)
 {
     if (callback) {
         auto changeEvent = reinterpret_cast<std::function<void(float)>*>(callback);
diff --git a/frameworks/core/components_ng/pattern/stage/stage_manager.cpp b/frameworks/core/components_ng/pattern/stage/stage_manager.cpp
index f50650ecfb1..66f62d29723 100644
--- a/frameworks/core/components_ng/pattern/stage/stage_manager.cpp
+++ b/frameworks/core/components_ng/pattern/stage/stage_manager.cpp
@@ -270,6 +270,14 @@ bool StageManager::PushPage(const RefPtr<FrameNode>& node, bool needHideLast, bo
     // flush layout task.
     if (!stageNode_->GetGeometryNode()->GetMarginFrameSize().IsPositive()) {
         // in first load case, wait for window size.
+#if defined(MAC_PLATFORM)
+        // ArkUI-X macOS (M1 格-c2): the window's real size arrives asynchronously after
+        // PushPage, so the stage is still 0x0 here. Mark the page subtree dirty for measure
+        // anyway, so when SetRootRect later applies the real size the page is re-measured
+        // (otherwise its geometry stays 0 and RS paints nothing).
+        stageNode_->MarkDirtyNode(PROPERTY_UPDATE_MEASURE);
+        node->MarkDirtyNode(PROPERTY_UPDATE_MEASURE);
+#endif
         return true;
     }
     stageNode_->MarkDirtyNode(PROPERTY_UPDATE_MEASURE);
diff --git a/frameworks/core/components_ng/pattern/symbol/bridge/symbol_glyph_dynamic_modifier.cpp b/frameworks/core/components_ng/pattern/symbol/bridge/symbol_glyph_dynamic_modifier.cpp
index 4cd0b7b1ea5..ec555667863 100644
--- a/frameworks/core/components_ng/pattern/symbol/bridge/symbol_glyph_dynamic_modifier.cpp
+++ b/frameworks/core/components_ng/pattern/symbol/bridge/symbol_glyph_dynamic_modifier.cpp
@@ -492,7 +492,7 @@ const ArkUISymbolGlyphModifier* GetSymbolGlyphDynamicModifier()
     return &modifier;
 }
 
-const CJUISymbolGlyphModifier* GetCJUISymbolGlyphModifier()
+const CJUISymbolGlyphModifier* GetCJUISymbolGlyphModifierStatic()
 {
     CHECK_INITIALIZED_FIELDS_BEGIN(); // don't move this line
     static const CJUISymbolGlyphModifier modifier = {
@@ -526,7 +526,7 @@ const CJUISymbolGlyphModifier* GetCJUISymbolGlyphModifier()
 
     return &modifier;
 }
-const ArkUISymbolGlyphCustomModifier* GetSymbolGlyphCustomModifier()
+const ArkUISymbolGlyphCustomModifier* GetSymbolGlyphCustomModifierStatic()
 {
     CHECK_INITIALIZED_FIELDS_BEGIN(); // don't move this line
     static const ArkUISymbolGlyphCustomModifier modifier = {
diff --git a/frameworks/core/components_ng/pattern/symbol/bridge/symbol_glyph_dynamic_module.cpp b/frameworks/core/components_ng/pattern/symbol/bridge/symbol_glyph_dynamic_module.cpp
index d160edd7b21..a1caff8179b 100644
--- a/frameworks/core/components_ng/pattern/symbol/bridge/symbol_glyph_dynamic_module.cpp
+++ b/frameworks/core/components_ng/pattern/symbol/bridge/symbol_glyph_dynamic_module.cpp
@@ -35,8 +35,8 @@ namespace OHOS::Ace {
 namespace NG {
 namespace NodeModifier {
 const ArkUISymbolGlyphModifier* GetSymbolGlyphDynamicModifier();
-const CJUISymbolGlyphModifier* GetCJUISymbolGlyphModifier();
-const ArkUISymbolGlyphCustomModifier* GetSymbolGlyphCustomModifier();
+const CJUISymbolGlyphModifier* GetCJUISymbolGlyphModifierStatic();
+const ArkUISymbolGlyphCustomModifier* GetSymbolGlyphCustomModifierStatic();
 }
 #ifdef INCLUDE_GENERATED_SOURCES
 namespace GeneratedModifier {
@@ -66,7 +66,7 @@ const void* SymbolGlyphDynamicModule::GetStaticModifier()
 
 const void* SymbolGlyphDynamicModule::GetCjModifier()
 {
-    return NG::NodeModifier::GetCJUISymbolGlyphModifier();
+    return NG::NodeModifier::GetCJUISymbolGlyphModifierStatic();
 }
 
 void* SymbolGlyphDynamicModule::GetModel()
@@ -77,7 +77,7 @@ void* SymbolGlyphDynamicModule::GetModel()
 
 const void* SymbolGlyphDynamicModule::GetCustomModifier(const std::string& name)
 {
-    return NG::NodeModifier::GetSymbolGlyphCustomModifier();
+    return NG::NodeModifier::GetSymbolGlyphCustomModifierStatic();
 }
 
 } // namespace OHOS::Ace
diff --git a/frameworks/core/components_ng/pattern/text_field/text_field_pattern.cpp b/frameworks/core/components_ng/pattern/text_field/text_field_pattern.cpp
index 22190ee7e47..902f0571520 100644
--- a/frameworks/core/components_ng/pattern/text_field/text_field_pattern.cpp
+++ b/frameworks/core/components_ng/pattern/text_field/text_field_pattern.cpp
@@ -1203,6 +1203,15 @@ void TextFieldPattern::UpdateCaretInfoToController(bool forceUpdate)
 #endif
     value.selection.Update(selectController_->GetStartIndex(), selectController_->GetEndIndex());
     InputMethodManager::GetInstance()->SetEditingState(value, GetInstanceId());
+#if defined(MAC_PLATFORM)
+    // Hand the caret rect (window pixels, top-left origin) to the platform IME so the
+    // macOS candidate window anchors at the caret. caretRect is relative to the text
+    // content; GetPaintRectGlobalOffset places that content within the window.
+    auto caretRect = selectController_->GetCaretRect();
+    auto globalOffset = GetPaintRectGlobalOffset();
+    InputMethodManager::GetInstance()->UpdateCursorInfo(caretRect.GetX() + globalOffset.GetX(),
+        caretRect.GetY() + globalOffset.GetY(), caretRect.Width(), caretRect.Height());
+#endif
 #else
     if (HasConnection()) {
         TextEditingValue value;
@@ -7267,6 +7276,12 @@ bool TextFieldPattern::ProcessFocusIndexAction()
 
 void TextFieldPattern::PerformAction(TextInputAction action, bool forceCloseKeyboard)
 {
+    // PerformAction can be invoked from a bare platform UI task (e.g. the macOS IME
+    // bridge posting the Return key) where no container is current. Without the
+    // scope the onSubmit JS callback fires against no active container, so its
+    // @State writes and RequestFrame are dropped and nothing re-renders. Mirror
+    // UpdateEditingValue, which already establishes the scope for the onChange path.
+    ContainerScope scope(GetInstanceId());
     if (!HasFocus()) {
         TAG_LOGW(AceLogTag::ACE_TEXT_FIELD, "Not Trigger OnSubmit because field blur");
         return;
@@ -10191,6 +10206,18 @@ void TextFieldPattern::UpdateSelectController()
 {
     selectController_->UpdateContentRect(contentRect_);
     selectController_->UpdateParagraph(paragraph_);
+#if defined(CROSS_PLATFORM)
+    // ArkUI-X (macOS): the deferred caret recompute (UpdateCaretRect / the
+    // after-layout task) can run against a stale paragraph or be skipped when the
+    // focusHub current-focus state lags behind the active IME connection, leaving
+    // the visual caret pinned at the line start. Recompute the caret rect here,
+    // where the fresh paragraph has just been propagated to the controller, so the
+    // caret tracks the text. Idempotent for the current index; skipped while
+    // selecting so it cannot collapse a selection.
+    if (paragraph_ && !contentController_->IsEmpty() && !IsSelected()) {
+        selectController_->MoveCaretToContentRect(selectController_->GetCaretIndex());
+    }
+#endif
 }
 
 bool TextFieldPattern::RepeatClickCaret(const Offset& offset, int32_t lastCaretIndex)
diff --git a/frameworks/core/components_ng/pattern/waterflow/bridge/waterflow/water_flow_dynamic_modifier.cpp b/frameworks/core/components_ng/pattern/waterflow/bridge/waterflow/water_flow_dynamic_modifier.cpp
index 9b8403c2595..fa5631defcd 100644
--- a/frameworks/core/components_ng/pattern/waterflow/bridge/waterflow/water_flow_dynamic_modifier.cpp
+++ b/frameworks/core/components_ng/pattern/waterflow/bridge/waterflow/water_flow_dynamic_modifier.cpp
@@ -1735,7 +1735,7 @@ const ArkUIWaterFlowModifier* GetWaterFlowDynamicModifier()
     return &modifier;
 }
 
-const CJUIWaterFlowModifier* GetCJUIWaterFlowModifier()
+const CJUIWaterFlowModifier* GetCJUIWaterFlowModifierStatic()
 {
     CHECK_INITIALIZED_FIELDS_BEGIN(); // don't move this line
     static const CJUIWaterFlowModifier modifier = {
diff --git a/frameworks/core/components_ng/pattern/waterflow/bridge/waterflow/water_flow_dynamic_module.cpp b/frameworks/core/components_ng/pattern/waterflow/bridge/waterflow/water_flow_dynamic_module.cpp
index 725d78f235c..efb0815185c 100644
--- a/frameworks/core/components_ng/pattern/waterflow/bridge/waterflow/water_flow_dynamic_module.cpp
+++ b/frameworks/core/components_ng/pattern/waterflow/bridge/waterflow/water_flow_dynamic_module.cpp
@@ -30,7 +30,7 @@ namespace OHOS::Ace {
 namespace NG {
 namespace NodeModifier {
 const ArkUIWaterFlowModifier* GetWaterFlowDynamicModifier();
-const CJUIWaterFlowModifier* GetCJUIWaterFlowModifier();
+const CJUIWaterFlowModifier* GetCJUIWaterFlowModifierStatic();
 } // namespace NodeModifier
 
 #ifdef INCLUDE_GENERATED_SOURCES
@@ -63,7 +63,7 @@ const void* WaterFlowDynamicModule::GetStaticModifier()
 
 const void* WaterFlowDynamicModule::GetCjModifier()
 {
-    return NG::NodeModifier::GetCJUIWaterFlowModifier();
+    return NG::NodeModifier::GetCJUIWaterFlowModifierStatic();
 }
 
 
diff --git a/frameworks/core/components_ng/render/adapter/rosen_window.cpp b/frameworks/core/components_ng/render/adapter/rosen_window.cpp
index 086a3e22416..c3b81968871 100644
--- a/frameworks/core/components_ng/render/adapter/rosen_window.cpp
+++ b/frameworks/core/components_ng/render/adapter/rosen_window.cpp
@@ -25,6 +25,12 @@
 #include "core/pipeline_ng/pipeline_context.h"
 #include "render_service_client/core/ui/rs_root_node.h"
 #include "render_service_client/core/ui/rs_surface_node.h"
+#if defined(MAC_PLATFORM)
+#include "render_service_client/core/pipeline/rs_render_thread.h"
+#include "render_service_client/core/ui/rs_ui_context.h"
+#include "transaction/rs_irender_client.h"
+#include "transaction/rs_transaction_proxy.h"
+#endif
 
 namespace {
 constexpr int32_t IDLE_TASK_DELAY_MILLISECOND = 51;
@@ -129,6 +135,29 @@ RosenWindow::RosenWindow(const OHOS::sptr<OHOS::Rosen::Window>& window,
             rsUIcontext->AttachFromUI();
             hasTaskRunner = rsUIcontext->HasTaskRunner();
     }
+#if defined(MAC_PLATFORM)
+    // ArkUI-X macOS (M1 格-c2): this is the director that actually renders the page tree
+    // (mac's Window::CreateSurfaceNode runs asynchronously after this ctor, so window->
+    // GetRSUIDirector() was null above and we created our own director). The private
+    // RSUIDirector::Init() never ran for it, so its transaction had no renderThreadClient_
+    // and every committed page tree was dropped. Wire the render-thread client + start the
+    // render thread on THIS director's context so RS receives and renders the page.
+    {
+        auto rtClient = OHOS::Rosen::RSIRenderClient::CreateRenderThreadClient();
+        if (rsUIcontext) {
+            auto transaction = rsUIcontext->GetRSTransaction();
+            if (transaction) {
+                transaction->SetRenderThreadClient(rtClient);
+            }
+        } else {
+            auto* proxy = OHOS::Rosen::RSTransactionProxy::GetInstance();
+            if (proxy) {
+                proxy->SetRenderThreadClient(rtClient);
+            }
+        }
+        OHOS::Rosen::RSRenderThread::Instance().Start();
+    }
+#endif
     if (!hasTaskRunner) {
         rsUIDirector_->SetUITaskRunner(
                 [taskExecutor, id](const std::function<void()>& task, uint32_t delay) {
diff --git a/frameworks/core/components_ng/render/adapter/rosen_window.h b/frameworks/core/components_ng/render/adapter/rosen_window.h
index ae41531d1dd..09ed7274703 100644
--- a/frameworks/core/components_ng/render/adapter/rosen_window.h
+++ b/frameworks/core/components_ng/render/adapter/rosen_window.h
@@ -23,7 +23,10 @@
 #include "wm/window.h"
 #elif VIRTUAL_RS_WINDOW
 // use real rs window later
-#ifdef IOS_PLATFORM
+// Phase 2 path X: mac defines both IOS_PLATFORM and MAC_PLATFORM; check MAC first.
+#if defined(MAC_PLATFORM)
+#include "adapter/macos/entrance/virtual_rs_window.h"
+#elif defined(IOS_PLATFORM)
 #include "adapter/ios/entrance/virtual_rs_window.h"
 #else
 #include "adapter/android/entrance/java/jni/virtual_rs_window.h"
diff --git a/frameworks/core/components_ng/render/adapter/txt_paragraph.cpp b/frameworks/core/components_ng/render/adapter/txt_paragraph.cpp
index f85f987d950..d1dc486c74f 100644
--- a/frameworks/core/components_ng/render/adapter/txt_paragraph.cpp
+++ b/frameworks/core/components_ng/render/adapter/txt_paragraph.cpp
@@ -754,6 +754,15 @@ bool TxtParagraph::IsIndexInEmoji(int32_t index, int32_t& emojiStart, int32_t& e
     if (!GetWordBoundary(index, start, end)) {
         return false;
     }
+    // ArkUI-X (macOS): the underlying paragraph can return a degenerate word
+    // boundary (e.g. (0,0)) for ordinary text. In that case GetRectsForRangeInner
+    // below would be queried over an empty/invalid range, come back empty, and the
+    // index would be misclassified as an emoji -- which collapses the caret extent
+    // to 0 and pins the caret at the line start. A real emoji always spans forward
+    // (end > index), so bail out of the emoji heuristic when it does not.
+    if (end <= index) {
+        return false;
+    }
     std::vector<RectF> selectedRects;
     // if index in emoji or the first or the last, selectedRects is empty and
     // 'end' will be emoji's end index or 0 or the max index.
diff --git a/frameworks/core/event/key_event_recognizer.cpp b/frameworks/core/event/key_event_recognizer.cpp
index 840f34f872d..382852318da 100644
--- a/frameworks/core/event/key_event_recognizer.cpp
+++ b/frameworks/core/event/key_event_recognizer.cpp
@@ -524,6 +524,12 @@ std::vector<KeyEvent> KeyEventRecognizer::GetKeyEvents(int32_t keyCode, int32_t
         if (static_cast<uint32_t>(metaKey) & static_cast<uint32_t>(CtrlKeysBit::ALT)) {
             addPressedKey(static_cast<int32_t>(KeyCode::KEY_ALT_LEFT));
         }
+        // META (e.g. the macOS Command key) drives the platform shortcut combos
+        // (KeyComb(KEY_V, KEY_META) etc.). Without this the Meta bit never becomes
+        // KEY_META_LEFT in pressedCodes, so Cmd+C/V/X/A never match on macOS.
+        if (static_cast<uint32_t>(metaKey) & static_cast<uint32_t>(CtrlKeysBit::META)) {
+            addPressedKey(static_cast<int32_t>(KeyCode::KEY_META_LEFT));
+        }
         addPressedKey(keyCode);
     }
     auto keyItems = getPressedKeys();
diff --git a/frameworks/core/interfaces/native/node/menu_modifier.cpp b/frameworks/core/interfaces/native/node/menu_modifier.cpp
index 0a50c7f5c93..c6a8e32c069 100644
--- a/frameworks/core/interfaces/native/node/menu_modifier.cpp
+++ b/frameworks/core/interfaces/native/node/menu_modifier.cpp
@@ -31,8 +31,12 @@ const ArkUIMenuModifier* GetMenuModifier()
     if (modifier == nullptr) {
         auto* module = DynamicModuleHelper::GetInstance().GetDynamicModule("Menu");
         if (module == nullptr) {
+            // ArkUI-X (cross-platform): the "Menu" dynamic module is not loaded on macOS, so degrade
+            // gracefully to nullptr instead of aborting. Every caller guards with CHECK_NULL_RETURN,
+            // so menu features no-op rather than crashing the whole app on e.g. a TextInput click
+            // (drag recognizer rejection -> IsGatherWithMenu -> CheckMenuManager -> here).
             LOGF("Can't find Menu dynamic module");
-            abort();
+            return nullptr;
         }
         modifier = reinterpret_cast<const ArkUIMenuModifier*>(module->GetDynamicModifier());
     }
@@ -46,8 +50,12 @@ const CJUIMenuModifier* GetCJUIMenuModifier()
     if (modifier == nullptr) {
         auto* module = DynamicModuleHelper::GetInstance().GetDynamicModule("Menu");
         if (module == nullptr) {
+            // ArkUI-X (cross-platform): the "Menu" dynamic module is not loaded on macOS, so degrade
+            // gracefully to nullptr instead of aborting. Every caller guards with CHECK_NULL_RETURN,
+            // so menu features no-op rather than crashing the whole app on e.g. a TextInput click
+            // (drag recognizer rejection -> IsGatherWithMenu -> CheckMenuManager -> here).
             LOGF("Can't find Menu dynamic module");
-            abort();
+            return nullptr;
         }
         modifier = reinterpret_cast<const CJUIMenuModifier*>(module->GetCjModifier());
     }
@@ -61,8 +69,12 @@ const ArkUIMenuInnerModifier* GetMenuInnerModifier()
     if (modifier == nullptr) {
         auto* module = DynamicModuleHelper::GetInstance().GetDynamicModule("Menu");
         if (module == nullptr) {
+            // ArkUI-X (cross-platform): the "Menu" dynamic module is not loaded on macOS, so degrade
+            // gracefully to nullptr instead of aborting. Every caller guards with CHECK_NULL_RETURN,
+            // so menu features no-op rather than crashing the whole app on e.g. a TextInput click
+            // (drag recognizer rejection -> IsGatherWithMenu -> CheckMenuManager -> here).
             LOGF("Can't find Menu dynamic module");
-            abort();
+            return nullptr;
         }
         modifier = reinterpret_cast<const ArkUIMenuInnerModifier*>(module->GetCustomModifier("menuInnerModifier"));
     }
@@ -75,8 +87,12 @@ const ArkUIMenuViewInnerModifier* GetMenuViewInnerModifier()
     if (modifier == nullptr) {
         auto* module = DynamicModuleHelper::GetInstance().GetDynamicModule("Menu");
         if (module == nullptr) {
+            // ArkUI-X (cross-platform): the "Menu" dynamic module is not loaded on macOS, so degrade
+            // gracefully to nullptr instead of aborting. Every caller guards with CHECK_NULL_RETURN,
+            // so menu features no-op rather than crashing the whole app on e.g. a TextInput click
+            // (drag recognizer rejection -> IsGatherWithMenu -> CheckMenuManager -> here).
             LOGF("Can't find Menu dynamic module");
-            abort();
+            return nullptr;
         }
         modifier =
             reinterpret_cast<const ArkUIMenuViewInnerModifier*>(module->GetCustomModifier("menuViewInnerModifier"));
@@ -90,8 +106,12 @@ const ArkUIMenuManagerInnerModifier* GetMenuManagerInnerModifier()
     if (modifier == nullptr) {
         auto* module = DynamicModuleHelper::GetInstance().GetDynamicModule("Menu");
         if (module == nullptr) {
+            // ArkUI-X (cross-platform): the "Menu" dynamic module is not loaded on macOS, so degrade
+            // gracefully to nullptr instead of aborting. Every caller guards with CHECK_NULL_RETURN,
+            // so menu features no-op rather than crashing the whole app on e.g. a TextInput click
+            // (drag recognizer rejection -> IsGatherWithMenu -> CheckMenuManager -> here).
             LOGF("Can't find Menu dynamic module");
-            abort();
+            return nullptr;
         }
         modifier =
             reinterpret_cast<const ArkUIMenuManagerInnerModifier*>(
