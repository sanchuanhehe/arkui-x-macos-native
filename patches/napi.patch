diff --git a/BUILD.gn b/BUILD.gn
index 9e8b5735..ba455771 100755
--- a/BUILD.gn
+++ b/BUILD.gn
@@ -29,7 +29,13 @@ config("ace_napi_config") {
   ]
   defines = []
   cflags_cc = []
-  if (is_mingw || is_mac || is_linux || ark_standalone_build) {
+  # Phase 0 path X: PREVIEW marks the previewer build. The native-macOS arkui-x
+  # build (is_arkui_x, appframework/iOS-style backend) is NOT the previewer and
+  # must NOT define PREVIEW — with it, JsMessageDispatcher::CallCurlFunction
+  # becomes a pure virtual that AceContainerSG never implements ("allocating an
+  # object of abstract class type 'AceContainerSG'"). The desktop previewer has
+  # is_arkui_x == false, so it still gets PREVIEW.
+  if ((is_mingw || is_mac || is_linux || ark_standalone_build) && !is_arkui_x) {
     defines += [ "PREVIEW" ]
   }
 
@@ -87,6 +93,13 @@ config("ace_napi_config") {
     cflags_cc += [ "-std=c++17" ]
   } else if (is_mac) {
     defines += [ "MAC_PLATFORM" ]
+    # Phase 0 path X: the native-macOS arkui-x build reuses the iOS engine paths
+    # (kevent instead of epoll, single-arg pthread_setname_np). Define IOS_PLATFORM
+    # so native_engine.cpp takes the BSD/Darwin branches; the desktop previewer
+    # (is_arkui_x == false) keeps the Linux/epoll path.
+    if (is_arkui_x) {
+      defines += [ "IOS_PLATFORM" ]
+    }
   } else if (is_linux) {
     defines += [ "LINUX_PLATFORM" ]
     cflags_cc += [ "-std=c++17" ]
